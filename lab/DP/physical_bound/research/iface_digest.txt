====================================================================================================
FAMILY: PCI Express (link-level flow-control credits; non-posted request Tags; requester completion tracking)
LOAD-BEARING CLAIM: In PCIe, the permission to have a transaction outstanding is a first-class hardware resource provisioned separately from buffer storage, and it - not buffering - has repeatedly been the binding throughput constraint: the non-posted Tag field was widened three times (8-bit/256 -> 10-bit/768 in the PCIe 4.0 ECN -> 14-bit/15,360 in PCIe 6.0) explicitly because the outstanding-request token count could no longer cover the growing round-trip bandwidth-delay product, and the same revision had to add Scaled Flow Control (1x/4x/16x) because the 8/12-bit credit-counter fields - pure counters, no storage - capped advertisable credits below what physical RX buffers could already hold; meanwhile the class that carries forward progress (completions) is architecturally EXEMPT from the credit budget (endpoints must advertise infinite completion credits, with ordering rules mandating bypass to avoid deadlock), exactly the out-of-pool reserved-tranche structure DP uses.
VERDICT: CONFIRMED
CORRECTED: In PCIe, permission to have a transaction outstanding (a Tag) is a hardware resource provisioned separately from buffer storage, and both non-storage token pools have repeatedly been binding throughput constraints: the non-posted Tag space was widened from its original 5-bit default (32) to 8-bit/256 (Extended Tag), then to 10-bit/768 in PCIe 4.0 (Draft 0.7, back-ported to 3.x by ECN), then to 14-bit tags in PCIe 6.0 flit mode (strongly-recommended valid range 15,360 = 16,384 minus 1,024 reserved encodings; a legacy carve-out permits up to 16,128) - the spec itself motivating this with a bandwidth x round-trip-time formula to keep "Tag availability from becoming a bottleneck"; the same PCIe 4.0 revision added Scaled Flow Control (1x/4x/16x) because the 8/12-bit credit-counter fields - pure counters, no storage - capped advertisable credits at 127 header / 2047 data (~32KB) regardless of physical RX buffer size. Completions are exempt from link credit flow control at requester edge ports specifically - endpoints and non-peer-to-peer root ports must advertise infinite completion credits, with requesters required to pre-reserve completion buffering, and ordering rules mandate that completions be able to pass non-posted requests to avoid deadlock - but this is not a fabric-wide architectural exemption: switches and peer-to-peer root complexes advertise finite completion credits, and PCIe 6.0 flit-mode merged FC can even charge completions to a shared posted-credit pool. The structure is requester-side pre-reservation plus guaranteed bypass at the edge, which resembles (but is not identical to) an out-of-pool reserved tranche.
VERIFY-REASONING: Every load-bearing mechanism checks out against source text I fetched myself, with one scoped overreach. (1) Tag widening for BDP: Broadcom techdocs says verbatim that 10-bit tags "increase the number of non-posted requests from 256 to 768" and "as latency increases, an increase in unique tags is required to maintain the peak performance at 16 GT/s"; the ChipEstimate article by Richard Solomon (PCI-SIG VP) confirms the 8->10-bit expansion in PCIe 4.0 Draft 0.7 with "a total of 768 tags available" because devices "might not be able to fully utilize the 16GT/s data rate with the existing limits on credits and outstanding transactions"; and the PCIe 6.2 Base Spec transaction-layer text (GitHub transcription, English column) carries the rationale in the spec itself: larger (10-bit or 14-bit) tags "can avoid Tag availability from becoming a bottleneck" at "very high rates of NPRs or very larg ...
DP-RELEVANCE: STRONGLY SUPPORTIVE, with one honest asymmetry. (1) Equal-token comparison is native practice here: PCIe practitioners already compare performance at different token budgets on FIXED silicon/buffers - the 10-bit-tag BIOS switch (256 vs 768 outstanding on the same NIC, Broadcom fetched quote) is literally an equal-hardware, different-permission-budget A/B, and IP configurators expose tag count and buffer size as separate knobs sized by different considerations (tags by RTT x bandwidth; buffers by RAM cost). So 'equal permission budget' is a comparison this domain recognizes as physically meaningful, because the token (tag/tracker entry, credit counter range) is the scarce resource that spec revisions kept widening. (2) DP's exact axis - one joint budget floating across a PAIR of flows vs a fixed split - maps onto PCIe's two levels: the requester tag pool is the floating end (one namespace shared across all flows, no mandated split; Xillybus uniqueness quote), while per-VC/per-type link credits are the fixed-split end (six independent accountings per VC, no cross-VC pooling in the base protocol; Xillybus VC quote). PCIe thus contains BOTH design points, and the fact that the wire-level credits chose static partition specifically for non-interference is the right foil for DP's claim that a floating pool wins when flows are asymmetric. (3) DP's reserved escape tranche has a direct, spec-mandated analog: the completion class is exempt from the credit pool (infinite credits at endpoints) and privileged by mandatory reordering, and that exemption alone carries deadlock freedom - same role as DP's reserve being sole carrier of forward progress. Caveats to state in the paper: PCIe has no mechanism that pools link CREDITS across VCs or across the two directions of a link (pooling exists at the requester-side tag/tracker level and inside implementations that carve per-type credits from one shared RX RAM), and PCIe tokens are end-to-end-per-link transaction permissions rather than per-hop flit buffers - so the fair citation is 'permission scarcer than storage, provisioned decoupled from storage, pooled at the requester (tags) but statically split on the wire (credits), with an exempt class carrying forward progress', not 'PCIe already does DP'.
----------------------------------------------------------------------------------------------------
  TOKEN: Link-level Flow Control credits: six independent types = {Posted, Non-Posted, Completion} x {Header, Data}, accounted separately per Virtual Channel. Advertised by the receiver via InitFC1/InitFC2 DLLPs at link-up and returned via UpdateFC DLLPs; consumed per TLP (1 header credit per TLP; data credi ...
  SCARCITY: Direct spec-history evidence that the TOKEN NUMBERSPACE, not buffer bytes, became the throughput bind: PCIe 4.0 Draft 0.7 (2016) added Scaled Flow Control because 'certain devices with specific workloads might not be able to fully utilize the 16GT/s data rate with the existing limits on credits and outstanding transactions' (Richard Solomon, Synopsys). The fix widened the token arithmetic (1x/4x/16x scaling), not buffers: PCIe 3.1's max of 127 header credits extends to 508/2032, and data credits from 2047 (~32KB) to 8188/32,752 (~512KB) - i.e., before the ECN, a receiver with >32KB of RX RAM per type physically could not advertise it as credits. Credits also gate every TLP on every hop (per- ...
  DECOUPLED: Yes, in both directions. (1) More storage than tokens: pre-Gen4 the 8/12-bit credit fields capped advertisable credits below what large RX buffers could hold - the motivation for Scaled FC. (2) Storage with ZERO tokens: infinite-credit mode - endpoints MUST advertise infinite completion credits, disabling credit flow control for that class entirely; the actual completion storage is then provisioned unilaterally by the requester (see completion-tracker token). Receiver provisions buffer and adver ...
  POOLING: NO architectural pooling on the wire: the six credit types are accounted independently, and each Virtual Channel has fully separate credit counters ('merely separate sets of data buffers having a separate flow control credits and counters') - a static split, chosen exactly so one class/channel cannot block another. Implementations may carve the per-type credits out of one shared physical RX RAM (common in FPGA/switch IP), but that sharing is invisible to the protocol [unverified background]. Forward-progress/deadlock story: the Completion class is the exempt reserve - endpoints must advertise infinite completion credits so 'an endpoint can't refuse to accept a completion TLP based upon flow control', and the ordering rules make reordering 'mandatory to avoid deadlocks' (posted/completions may pass stalled non-posted). So PCIe's deadlock freedom rests on (a) a message class exempted from  ...
  RESERVE: 
  QUOTES: {'fetched-fulltext': 7}
----------------------------------------------------------------------------------------------------
  TOKEN: Non-posted request TAG (Transaction ID = Requester ID + Tag). One tag is the permission to have one read/non-posted request outstanding; it is allocated by the requester at issue and freed when the final completion returns. History: 5-bit default (32 tags) -> 8-bit Extended Tag (256) -> 10-bit Tag E ...
  SCARCITY: The cleanest bandwidth-delay-product scarcity story in any interconnect: the tag field has been widened three times, each time explicitly because outstanding-request permissions ran out before link bandwidth did. Gen4 ECN rationale (Solomon/Synopsys): devices 'might not be able to fully utilize the 16GT/s data rate with the existing limits on credits and outstanding transactions', so the Tag field went 8->10 bits (768 tags; one encoding reserved). Synopsys PCIe 5.0: 'Selecting too few tags has an adverse effect on performance. As total roundtrip transmit time, or latency increases, so does the number of tags required to maintain the maximum performance at 32 GT/s.' Synopsys PCIe 6.0: 'the pr ...
  DECOUPLED: Fully decoupled - a tag is a namespace entry carrying zero bytes of storage. The storage that backs it (the completion/reorder buffer) is provisioned separately by the requester, because completions flow with infinite link credits: 'the Requester of a non-posted transactions must take responsibility for being able to accept the completion by verifying that it has enough buffer space when making the request' (Xillybus). Conversely the completer explicitly does NOT need buffer per tag: designers ' ...
  POOLING: The tag space is a SINGLE POOLED BUDGET, floating by default across all of a requester's concurrent flows: the spec imposes no per-destination or per-direction partition - 'the standard doesn't require a certain enumeration method, as long as the Tags of all outstanding requests are unique' (Xillybus). A DMA engine reading from N targets draws all its read permissions from one shared tag/tracker pool and can shift them between flows cycle by cycle; fixed per-queue tag carving is purely an implementation choice. Forward-progress story: tags themselves cannot deadlock the fabric because the resource they meter (completions) is exempt from credit flow control and privileged by the ordering rules; the pool drains as long as completions can flow, which the infinite-credit + mandatory-reordering rules guarantee. So PCIe pairs a fully-floating permission pool (tags) with an out-of-pool guarante ...
  RESERVE: Not a per-tag reserve; the escape path is class-level: completions are exempt from credit accounting (infinite credits at endpoints) and ordering rules make bypass of stalled non-posted requests mandatory, so returning completions (which free tags) can always make progress.
  QUOTES: {'fetched-fulltext': 6}
----------------------------------------------------------------------------------------------------
  TOKEN: Requester completion buffer / outstanding-read admission (self-imposed limit: outstanding reads x Max_Read_Request_Size must fit the requester's completion/reorder RAM, since completions arrive with no link-credit protection).
  SCARCITY: This is the storage-side dual of the tag token: because endpoints must advertise infinite completion credits, the ONLY thing protecting the requester from completion overrun is its own admission control - so its usable tag budget is bounded by completion RAM, and vendor read-throughput app notes (e.g., Intel FPGA 'Throughput for Reads') derive achievable read bandwidth from outstanding-request count x request size vs RTT [that specific doc returned HTTP 403; claim labeled accordingly]. The fetched Xillybus quote establishes the mechanism itself.
  DECOUPLED: This resource IS the storage, and it is provisioned by the opposite party from the link token: receiver-of-completions (the requester) buys RAM; the link carries no completion credits. A design can hold more completion RAM than tags (tags bind) or more tags than RAM (RAM binds and the app layer must throttle) - the two budgets are set independently, which is why IP tools expose them as separate configuration knobs.
  POOLING: The completion buffer is typically one shared RAM pooled across all outstanding tags/flows of the requester, drained in completion-arrival order; no architectural per-flow reserve (implementation-specific) [unverified background beyond the quoted requester-responsibility rule].
  RESERVE: 
  QUOTES: {'fetched-fulltext': 1, 'search-rendered': 1}
SAVED: D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/pcie_synopsys_pcie6_tags.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/pcie_synopsys_pcie5_10bit_tags.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/pcie_chipestimate_synopsys_gen4_scaled_credits.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/pcie_xillybus_part2_flow_control_credits.txt;  ...
====================================================================================================
FAMILY: Arm AMBA CHI (protocol-layer PCrd + link-layer L-Credit + TxnID), AMBA AXI (IDs + issuance/acceptance limits + AXI3 write-interleave depth), ...
LOAD-BEARING CLAIM: In CHI, the permission to hold an outstanding request at a Home Node is an explicit, protocol-visible credit (RetryAck -> PCrdGrant with PCrdType classes) that is decoupled from both link buffering and data storage, and Arm's shipping HN-F implementation (CMN-700 POCQ) manages exactly a single shared tracker-entry pool with per-class dedicated reserves that are exempt from sharing ("Unused dedicated entries belonging to a class are unavailable for other classes to use"), per-class max_allowed caps that retry requests "even if POCQ has available entries", and credit-grant arbitration ? while the tracker itself, not buffer bytes, is the documented scarce hardware (">64 depths may have frequency implications"; CBusy throttles the outstanding-transaction count; special optimizations exist "to reduce tracker lifetime and increases the number of outstanding requests").
VERDICT: CONFIRMED
CORRECTED: In CHI, permission to hold an outstanding request at a Home Node is an explicit, protocol-visible credit (RetryAck -> PCrdGrant with matching implementation-defined PCrdType classes) that is a separate mechanism from link-layer L-credit flow control and is architecturally agnostic to what resource backs it (the protocol permits, e.g., data buffers per credit type; in Arm's CMN-700 the backing resource is tracker entries). CMN-700's HN-F POCQ manages a single tracker-entry pool with per-class dedicated reserves exempt from sharing ("Unused dedicated entries belonging to a class are unavailable for other classes to use"), per-class max_allowed caps that retry requests "even if POCQ has available entries", and multi-tier credit-grant arbitration (dedicated -> contended_min -> max_allowed). The tracker, not buffer bytes, is the documented scarce hardware: the NUM_ENTRIES_POCQ parameter warns ">64 depths may have frequency implications", and HNF CBusy encodes POCQ tracker-occupancy thresholds so RNs throttle outstanding requests (with static outstanding-transaction caps toward SN-F). The "reduce tracker lifetime and increases the number of outstanding requests" optimization is real but documented for the CCG/PCIe RN-I write path, not the HN-F POCQ itself.
VERIFY-REASONING: I downloaded both load-bearing primary sources in full and checked every quoted phrase against the extracted text. (1) CHI protocol credit: the Arm "Introducing AMBA CHI" guide (102407_0102_01_en) describes link-layer L-credits (LCRDV-based flit crediting) and the Request Retry mechanism as separate layers: a first-issue request has AllowRetry=1/PCrdType=0; a resource-poor Home responds RetryAck with a PCrdType; the requester may retry only after a PCrdGrant with matching PCrdType ? an explicit, protocol-visible credit whose types "track different resources" with implementation-defined values. (2) CMN-700 TRM r3p2 (102308_0302_07_en, Issue 07, 1306 pp): all three POCQ quotes are verbatim in sec 5.5.2 ? "Unused dedicated entries belonging to a class are unavailable for other classes to use"; max_allowed causes retry "even if POCQ has available entries"; contended_min priority for "credit  ...
DP-RELEVANCE: Strong, with a precise mapping. DP's core comparison ? equal permission budget S, floating vs fixed split across a PAIR of flows, with a reserved escape tranche exempt from the pool ? is not just recognizable to an AMBA practitioner, it is a register-programmable configuration sweep on shipping Arm silicon: on a CMN-700 HN-F, setting cmn_hns_pocq_alloc_class_dedicated = 0 with max_allowed = S gives a fully floating shared tracker pool, setting dedicated = S/2 per class gives a hard fixed split, and every point between is legal; contended_min adds a soft reserve and max_allowed an anti-hogging cap, and Arm ships PMU events (pocq occupancy, hnf_pocq_retry, per-class occupancy) specifically so integrators can tune this split. The forward-progress story matches DP exactly: the dedicated tranche is exempt from sharing and alone guarantees each class acceptance, while a granted credit (static request) "cannot be retried" ? reserve carries progress, pool carries throughput. Scarcity is also the right kind: tracker/credit counts, not buffer bytes, are the documented binding constraint (POCQ >64 entries has frequency implications; L-credits are sized to credit roundtrip latency, i.e., outstanding = BDP; CBusy congestion control actuates on outstanding-transaction count; CHI widened TxnID 8->10 bits, 256->1024 outstanding, the classic tag-extension signature; AXI issuance=1 "does not permit transaction pipelining" and raising issuance/acceptance counters is the standard bandwidth fix). Two honest caveats for the paper: (1) the pooled token in this domain lives at the PROTOCOL layer (tracker entries / PCrd / issuance counters) ? CHI link-layer L-Credits are strictly per-channel, buffer-backed, and never pooled across directions, so DP should be framed as importing the protocol-layer shared-tracker-with-reserves pattern down into router flow control, not as a variant of L-crediting; (2) the CMN pool is shared across QoS classes at ONE node, whereas DP pools across two opposing inports of a dimension ? the budget-sharing structure (shared pool + exempt per-class/per-inport reserve + per-class cap) is isomorphic, but the sharers differ (classes vs directions), which is exactly the delta DP can claim as novel.
----------------------------------------------------------------------------------------------------
  TOKEN: CHI Protocol Credit (PCrd), granted via RetryAck + PCrdGrant (PCrdType classes) ? the permission to occupy a Home Node request-tracker entry
  SCARCITY: Tracker entries, not buffers, are the documented binding resource: (1) CMN-700 config table caps POCQ at 128 entries and warns '> 64 depths may have frequency implications' ? tracker depth is timing-limited hardware. (2) CMN-700's CBusy congestion mechanism throttles the OUTSTANDING-TRANSACTION COUNT (not bandwidth or buffers) as its actuator, in static or dynamic mode, with the not-busy ceiling defined as 'as many requests as the number of POCQ entries'. (3) CMN-700 adds a special early-completion optimization for PCIe writes explicitly 'to reduce tracker lifetime and increases the number of outstanding requests' ? occupancy-time of tracker entries is the throughput constraint being enginee ...
  DECOUPLED: Yes, cleanly. The first-attempt request always physically traverses the link and its flit buffering uncredited (AllowRetry=1); the PCrd only permits occupancy of a completer-side tracker entry. Learn-CHI states credit types can map to arbitrary internal resources ('Read Requests and Write Requests can use separate Data Buffers, so each buffer can use a different type of Protocol Credit'). In CMN-700 the POCQ tracker (16-128 entries) is provisioned independently of data buffers (e.g., SBSX has se ...
  POOLING: This is the canonical shared-pool-plus-per-class-reserve design. CMN-700 POCQ is ONE pool shared across all requesters and all four QoS/opcode classes, governed per class by: dedicated (hard reserved entries, exempt from sharing ? unused dedicated entries are NOT lendable to other classes), max_allowed (per-class cap enforced even when the pool has free entries, i.e., anti-hogging), and contended_min (soft reserve: not reserved entries, but priority in credit-grant arbitration when below it). Credit grants to retried requesters are arbitrated: classes below dedicated first (RR), then below contended_min (RR), then below max_allowed (WRR); within a class, RR across RNs. Forward progress: a retried request that returns with a granted credit is a 'static' request that CANNOT be retried ? a granted token is a guaranteed acceptance; Arm recommends max_allowed > contended_min > dedicated.
  RESERVE: Yes, explicit and load-bearing: per-class dedicated POCQ entries are reserved and exempt from the shared pool ('Unused dedicated entries belonging to a class are unavailable for other classes to use'), plus a separate SEQ reserve for snoop-filter evictions; the TRM constrains total dedicated + SEQ <= POCQ size. Combined with credited-retry = guaranteed allocation, this is exactly a reserved tranche carrying forward progress while the remainder floats.
  QUOTES: {'fetched-fulltext': 13}
----------------------------------------------------------------------------------------------------
  TOKEN: CHI link-layer L-Credit (per-channel: REQ/RSP/DAT/SNP)
  SCARCITY: Explicit bandwidth-delay-product sizing rule in an Arm production TRM: the minimum number of L-Credits needed to avoid pipeline stalls equals the credit roundtrip latency (i.e., credits = BDP in flits), and the count is small (1-15, default 4). Credits are expensive because each must be backed by receiver flit buffering ('must not exceed its flit buffering and processing capabilities'), so designers issue only as many as latency requires.
  DECOUPLED: One-way coupled: credits issued <= receiver buffer capacity is mandatory (tokens can never exceed storage), but storage may exceed tokens. The receiver provisions both and unilaterally decides how many credits to expose; the transmitter just counts. So at the link layer the token is a conservative proxy for storage, not an independent resource.
  POOLING: None. L-Credits are strictly per-channel and per-link; the four channels are architecturally independent precisely so that response/data progress can never be blocked by request congestion (channel independence is part of the protocol's deadlock-avoidance story). No CHI mechanism floats L-Credits across channels, directions, or virtual networks ? pooling in the CHI stack lives one layer up, in Protocol Credits/tracker entries.
  RESERVE: Not applicable at this layer ? deadlock freedom comes from fixed channel separation (each channel has its own credits/buffers), the opposite of pooling.
  QUOTES: {'fetched-fulltext': 4}
----------------------------------------------------------------------------------------------------
  TOKEN: CHI TxnID ? the outstanding-transaction tag space of a Request Node
  SCARCITY: Direct tag-field-extension history in the architecture itself: TxnID was an 8-bit field (256 outstanding per RN) and later CHI issues widened it to allow 1024 outstanding ? Arm's official guide states the outstanding limit depends 'on the specific CHI issue compliance'. Widening a field in every flit of every channel is expensive (wires + tracker depth), done only because 256 outstanding capped bandwidth-delay product at modern memory latencies. (Contextual interpretation of the widening motive is background knowledge; the widening itself is quoted below.)
  DECOUPLED: Yes ? a namespace, not a buffer: an RN can implement any tracker depth up to the tag-space bound, and buffers (e.g., its fill/writeback data storage) are provisioned separately. The requester provisions its own tag pool and tracker; no receiver storage is reserved per TxnID.
  POOLING: The tag space is inherently one pooled budget across all of an RN's request flows and message types (reads, writes, dataless) ? there is no architectural per-class split of TxnIDs; DBID is a second, completer-owned tag namespace for the write-data/ack direction, illustrating per-direction token ownership.
  RESERVE: None at tag level; forward progress relies on the protocol-layer retry/credit mechanism and channel independence, not on reserved tags.
  QUOTES: {'fetched-fulltext': 2}
----------------------------------------------------------------------------------------------------
  TOKEN: AXI transaction IDs (ARID/AWID 'ID threads') + issuance/acceptance limits (outstanding-transaction counters) + AXI3 write-interleave depth
  SCARCITY: The outstanding-transaction counter is THE documented AXI bandwidth knob: Xilinx's AXI reference guide lists 'multiple outstanding addresses, and out-of-order transaction processing' among the features for 'very high data throughput', notes the default issuance of 1 'does not permit transaction pipelining', and walks through computing how many outstanding transactions the datapath FIFOs can support. Tracker slicing in CMN-700 (64-entry slices for timing) shows tracker depth is frequency-limited hardware, same as POCQ. Negative-direction evidence: AXI3 write interleaving ? a token-per-stream concurrency feature ? was RETRACTED in AXI4 because the per-stream tracking/reordering hardware wasn't ...
  DECOUPLED: Yes ? issuance/acceptance are pure counters configured per port, independent of (but sanity-checked against) datapath FIFO sizing; the guide explicitly treats 'how many tokens' and 'how much FIFO' as separate provisioning steps ('you can calculate the maximum number of outstanding transaction possible without overflowing the data path FIFOs'). Master IP provisions issuance capability; interconnect/slave provisions acceptance and storage.
  POOLING: No floating pool: issuance/acceptance limits are static per port. Sharing happens at the arbiter ? a port that exhausts its acceptance/issuing limit is 'temporarily disqualified from arbitration, so that other SI slots can be granted arbitration' (work-conserving sharing of datapath, fixed split of tokens). Deadlock story is ID-routing-based, not reserve-based: the 'Single-Slave per ID' rule restricts each ID thread to one slave at a time to break cyclic dependencies. AXI has no retry/credit-grant recovery: a stalled channel simply backpressures, which is why CHI added the retry layer (secondary observation).
  RESERVE: None. AXI avoids deadlock by ordering restrictions per ID (Single-Slave per ID) and by never overcommitting (blocking) rather than by reserved token tranches.
  QUOTES: {'fetched-fulltext': 8}
----------------------------------------------------------------------------------------------------
  TOKEN: TileLink source identifier (a_source / c_source) ? bonus
  SCARCITY: The spec states the purpose of the uniqueness constraint outright: it exists so agents can bound tracking-state hardware ? the number of usable source IDs IS the outstanding-transaction budget, and slave state grows with it. (In the Rocket/Diplomacy ecosystem, sourceId ranges are negotiated at elaboration and directly set tracker sizes ? background knowledge, unverified here.)
  DECOUPLED: Yes ? TileLink link-level flow control is plain ready/valid backpressure per channel (buffering is a private implementation choice at each end), while the source-ID pool independently bounds concurrency. A design can trivially have more buffering than IDs or more IDs than buffer slots; masters provision IDs, each hop provisions its own queues.
  POOLING: Static per-channel namespace partitioning, no dynamic pooling: A-channel and C-channel requests draw from separate namespaces ('an inflight A and C request can each use the same value'), i.e., per-message-class token budgets are fixed-split by construction. Forward progress/deadlock is carried not by token reserves but by the strict channel priority ordering A < B < C < D < E.
  RESERVE: None token-based; deadlock freedom comes from the mandated channel priority hierarchy (quoted) plus the rule that responses are guaranteed sinkable.
  QUOTES: {'fetched-fulltext': 3}
SAVED: D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/chi_learn_amba_chi_102407.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/chi_cmn700_trm_102308.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/chi_ihi0050_errata_ea.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/axi_ug1037_vivado_axi_reference_guide.txt; D:/THU/sophomore_spring/AI_X/_fukase ...
====================================================================================================
FAMILY: Intel QPI/UPI link layer (VN0/VN1/VNA credit pools) and Intel uncore mesh (transgress/UPI-ingress/shared-vs-dedicated credits)
LOAD-BEARING CLAIM: Intel's shipping QPI/UPI link layer makes the credit -- not the physical buffer -- the flow-control commodity: one Rx-buffer credit budget is split, purely by counters over a physically shared flit buffer, into a large shared adaptive pool (VNA) used by all six message classes plus small per-message-class dedicated VN0/VN1 reserves that alone guarantee deadlock freedom, with hardware trying the pool first and falling back to the reserve -- verbatim: "There are two credit pools, VNA and VN0. VNA is a shared pool used to achieve high performance. The VN0 pool has reserved entries for each message class and is used to prevent deadlock. Requests first attempt to acquire a VNA credit, and then fall back to VN0 if they fail." -- and Intel documents pool exhaustion (VNA_CREDIT_CYCLES_OUT) as the event that "can substantially throttle back useful QPI bandwidth" while VN0 holders "will still (potentially) be able to transmit."
VERDICT: CONFIRMED
CORRECTED: Intel's shipping QPI/UPI link layer makes the credit ? not the physical buffer ? the flow-control commodity: the receiver's flit-buffer capacity is exposed to the transmitter as two credit pools ? a large shared adaptive pool (VNA) used by all six message classes and providing "the bulk of the QPI bandwidth," plus per-message-class dedicated VN0/VN1 reserves whose role is deadlock prevention / guaranteed forward progress ? with hardware trying the pool first and falling back to the reserve, verbatim: "There are two credit pools, VNA and VN0. VNA is a shared pool used to achieve high performance. The VN0 pool has reserved entries for each message class and is used to prevent deadlock. Requests first attempt to acquire a VNA credit, and then fall back to VN0 if they fail." Credit granularity is decoupled from buffer slots (a VNA credit maps to one flit buffer while a single VN0 credit grants a whole packet's worth of flit buffers), and Intel documents pool exhaustion (VNA_CREDIT_CYCLES_OUT) as an event that "can substantially throttle back useful QPI bandwidth" while VN0 holders "will still (potentially) be able to transmit." Caveat dropped from the original: Intel's public docs do not state that the split is done "purely by counters over a physically shared flit buffer" ? the UPI-era guide's "passes flits through shared buffers" and the fact that VNA/VN0/VN1 credits all draw on "the Rx buffer" support shared physical buffering as a plausible microarchitectural reading, but the QPI white paper itself describes VN0/VN1 as "independently buffered" in contrast to the "shared adaptive buffered" VNA, so physical sharing of the reserves is an inference, not spec text; likewise deadlock freedom is attributed by Intel to message classes and virtual networks together, with the VN0/VN1 reserved credits as the credit-level forward-progress guarantee.
VERIFY-REASONING: Fetched and read all five cited sources directly (WebFetch was blocked; downloaded PDFs/HTML via curl and extracted text with pdftotext). (1) The verbatim quote is exact: Intel doc 329468-002 (Xeon E5-2600 v2 uncore manual), R3QPI chapter, VN0_CREDITS_REJECT and VN0_CREDITS_USED definitions contain word-for-word "There are two credit pools, VNA and VN0. VNA is a shared pool used to achieve high performance. The VN0 pool has reserved entries for each message class and is used to prevent deadlock. Requests first attempt to acquire a VNA credit, and then fall back to VN0 if they fail." The same text appears with VN1 substituted, on the live perfmon-events.intel.com Broadwell-X page (6 hits), and nearly identically in the UPI-era EMR guide 817509 (M3UPI VN0_CREDITS_USED: "In order for a request to be transferred across Intel UPI... There are two credit pools, VNA and VN0...") ? so QPI->UPI p ...
DP-RELEVANCE: STRONG YES -- this family is the closest shipping analogue to DP, with one honest asymmetry. (1) Recognizable comparison: the design question Intel answered in silicon is literally "given one credit budget over one Rx buffer, how much floats in a shared pool (VNA) vs how much is fixed per class (VN0/VN1)?" -- so "equal permission budget, floating vs fixed split across flows" is not an artificial axis but the native provisioning axis of this domain; the QPI intro even names the pool's purpose as buffer economy ("a reduction in required buffering through adaptively buffered virtual networks"). A DP experiment at equal token budget comparing a pooled counter against a static per-inport split is exactly the VNA-vs-VN0-only comparison an uncore architect already reasons about. (2) Permission is the observable, binding resource: Intel's PMU instruments credits (consumed/rejected/cycles-out/pending-return), the manual declares VNA credit exhaustion the thing that "can substantially throttle back useful QPI bandwidth", the M3UPI arbiter is built around remote-credit-level thresholds (<4, <5, <10) of a pool only ~one data packet deep in margin, and at the protocol layer Intel widened the token system itself (QPI preallocated home-tracker quotas -> UPI "no preallocation to remove scalability limits") rather than adding storage -- a shipped fixed-split-to-pool transition. (3) Deadlock architecture is isomorphic to DP: a reserved per-class tranche exempt from the pool (VN0 reserved entries "used to prevent deadlock"/"guarantee forward progress", still usable at VNA==0) alone carries forward progress, exactly like DP's escape tranche; the same pool+reserve pattern recurs at the iMC (regular vs "special" ISOCH credits) and in mesh shared-vs-dedicated P2P credits. (4) The honest caveat to state in the paper: in UPI the pooled tokens ride over ONE physically shared Rx buffer (each credit conservatively backed by storage; the VNA/VN0 partition exists only in counters), whereas DP pools tokens over PRIVATE per-inport buffers -- so DP is the dual arrangement (shared counter / private RAM vs shared RAM / partitioned counters). What transfers is precisely DP's premise: the split of the budget is enforced ONLY by counters, the counter budget (not buffer construction) is what architects size and what the PMU watches, token granularity is already decoupled from storage granularity (1 VN0 packet-token ~ 9 flit buffers), and equal-token-budget floating-vs-fixed is the comparison this domain has run in production for 15+ years.
----------------------------------------------------------------------------------------------------
  TOKEN: VNA credit (Virtual Network Adaptive) ? flit-granular entry permission into the shared QPI/UPI link-layer Rx flit buffer, shared by ALL message classes
  SCARCITY: Intel documents credit exhaustion, not buffer occupancy, as the bandwidth limiter: a dedicated PMU event counts cycles with zero VNA credits and the manual states the uncore's goal is that VNA credits 'should not run out, as this can substantially throttle back useful QPI bandwidth'. The pool is small relative to demand: a single data transfer consumes up to 9 flit credits (E5 v2 manual), and on 5th-gen Xeon (EMR) the M3UPI arbiter degrades stepwise as the remote VNA level falls below 10 (parallel VN0/VN1 arbitration disabled), below 5 (parallel AD/BL arbitration disabled), and below 4 (a BL message cannot arbitrate at all) ? i.e., the shipping arbiter is engineered around a credit level of  ...
  DECOUPLED: Partially decoupled, and the partition is counter-only: the receiver's Rx flit buffer is one physically shared structure ('The Intel UPI link layer passes flits through shared buffers, and manages their flow control' ? EMR guide), and the VNA/VN0/VN1 split of that buffer exists only in the credit counters, not in physical partitioning. Granularity is also decoupled: a VNA credit = 1 flit buffer, while a VN0/VN1 credit = 1 packet that may occupy ~9-11 flit buffers ('a transfer that uses VNA could ...
  POOLING: VNA is the pooling mechanism: one adaptive credit pool shared by all six message classes (SNP/HOM/NDR/DRS/NCS/NCB) and, in UPI, by traffic of both VN0 and VN1 on the AD and BL paths (EMR RxC_VNA_CRD_MISC distinguishes 'remote VNA credits were allocated only to VN0, not to VN1', etc.). Allocation protocol: 'Requests first attempt to acquire a VNA credit, and then fall back to VN0 if they fail.' Forward-progress/deadlock story: the dedicated per-message-class VN0 (and VN1) reserve is exempt from the pool and alone carries deadlock freedom ? 'The VN0 pool has reserved entries for each message class and is used to prevent deadlock'; when VNA is exhausted 'those holding VN0 credits will still (potentially) be able to transmit.' Intel's stated motivation for VNA is buffer economy: 'a reduction in required buffering through adaptively buffered virtual networks.'
  RESERVE: VN0 (and optionally VN1): 'up to two independently buffered virtual networks (VN0 and VN1) and one shared adaptive buffered virtual network (VNA)'; VN0 has reserved entries per message class, is used 'to guarantee forward progress', and remains usable when the VNA pool is empty. This is exactly a per-class escape tranche exempt from the shared pool.
  QUOTES: {'fetched-fulltext': 9}
----------------------------------------------------------------------------------------------------
  TOKEN: VN0 / VN1 credit ? packet-granular, per-message-class DEDICATED credit (the deadlock/escape reserve of the QPI/UPI link layer)
  SCARCITY: Deliberately minimal: 'reserved entries for each message class' ? one packet-credit tranche per class, small enough that a VN0 credit is packet-granular precisely to keep the reserve cheap ('a single VN0 credit holds access to potentially multiple flit buffers'). The manual expects the double-failure case (neither VNA nor VN0 credit) to be 'a rare situation'. VN1 exists only for topologies that need a second independent network (multi-hop gluelessly-routed systems); Table on EMR shows VN1 events still present on 8-socket-capable parts.
  DECOUPLED: Yes, in granularity: one VN0 token maps to a variable amount of physical storage (1 header flit up to ~9-11 flit buffers for a data packet), so token count and buffer count are different currencies; the VN0 'entries' are reserved out of the shared Rx buffer purely by the credit accounting.
  POOLING: Explicitly NOT pooled ? this is the reserve that makes the VNA pool safe. Fallback order is fixed in hardware: 'Requests first attempt to acquire a VNA credit, and then fall back to VN0 if they fail.' The reserve is exempt from pool exhaustion: with zero VNA credits, 'those holding VN0 credits will still (potentially) be able to transmit.' Message classes + virtual networks 'together are used to prevent protocol deadlocks' (QPI intro).
  RESERVE: This token IS the reserve: 'The VN0 pool has reserved entries for each message class and is used to prevent deadlock.'
  QUOTES: {'fetched-fulltext': 3}
----------------------------------------------------------------------------------------------------
  TOKEN: Uncore mesh-stop credits: transgress (TGR) credits per mesh row/direction, CHA-held credits for the UPI ingress buffer, and 'shared' vs 'dedicated' P2P credits at M2PCIe/M3UPI
  SCARCITY: Intel dedicates PMU events specifically to permission starvation, not buffer fullness: per-agent, per-path stall events ('Number of cycles the AD agent 0 egress buffer is stalled waiting for a TGR credit to become available, per transgress') and an injection-starvation mode triggered by lack of credit ('the Ingress is unable to forward to the egress due to a lack of credit'). The mesh chapter opens by listing 'Credit tracking and stalls due to lack of credits' as the first thing the common mesh events exist to measure.
  DECOUPLED: Yes: the CHA acquires a credit for a buffer that lives in a different unit possibly several mesh hops away ('it is necessary to first acquire a credit for the UPI ingress buffer'), so the token counter and the storage it represents are in different boxes; and some mesh traffic flows entirely uncredited (bounce/ring traffic), i.e., storage exists that no token governs.
  POOLING: The event taxonomy itself shows Intel provisions permission as pool + reserve across the mesh: distinct LOCAL/REMOTE 'Shared P2P Credit' and 'Dedicated P2P Credit' taken/returned/wait event families in M2PCIe and M3UPI (EMR guide lists them, e.g. 'Remote Shared P2P Credit Taken - 0', 'Local Dedicated P2P Credit Taken - 0', 'Waiting on Remote Shared P2P Credit - 0', 'Local Shared P2P Credit Returned to credit ring'); NOTE: in the EMR guide these P2P entries carry event names/codes but no prose definitions, so the shared-vs-dedicated semantics are inferred from the official event names only. The same pool+reserve pattern appears with prose at the iMC interface: the read/write pending queues are 'broken into regular credits/buffers that are used by general reads, and "special" requests such as ISOCH reads' ? a general pool plus a reserved tranche for the traffic class that must not starve.
  RESERVE: Per-class dedicated credits alongside shared pools (P2P DED vs SHAR; iMC 'special' ISOCH credits vs 'regular' credits); the manual notes it is 'difficult (and uncommon) to make use of both the regular and special buffers at the same time', i.e., the reserve is normally idle capacity paid for guaranteed progress.
  QUOTES: {'fetched-fulltext': 5}
----------------------------------------------------------------------------------------------------
  TOKEN: Protocol-layer outstanding-transaction permission: home-agent Tracker File entries, preallocated per caching agent in QPI, preallocation ABOLISHED in UPI
  SCARCITY: Intel names the tracker, not a buffer, as THE critical resource: 'TF is the critical resource each transaction needs before being sent to the iMC.' And the headline QPI-to-UPI protocol change was widening/pooling this permission: preallocation was removed because it imposed 'scalability limits' ? i.e., the fixed per-source split of outstanding-transaction tokens, not storage, was the binding constraint, and Intel fixed it by changing token allocation policy rather than by adding storage.
  DECOUPLED: Yes: a tracker entry is a permission-to-be-outstanding (a tag/scoreboard slot), consumed at request issue and held until completion, independent of any data buffering; QPI provisioned it as preallocated per-agent quotas, UPI provisions it dynamically ? same storage concept, different token policy across two shipping generations.
  POOLING: QPI = fixed split (preallocated per caching agent); UPI = pooled/dynamic ('Intel UPI removes the requirement on preallocation of resources at the home agent'). This is a shipped, documented transition from fixed token split to floating token pool at (conceptually) the same tracker hardware ? the exact axis DP's experiments vary. Forward progress in UPI moves to the link-layer VN0 reserves and the coherence protocol's conflict-resolution machinery rather than per-source guaranteed tracker slots.
  RESERVE: 
  QUOTES: {'fetched-fulltext': 3}
SAVED: D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/intel_qpi_upi_qpi_intro_whitepaper.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/intel_qpi_upi_e5v2_uncore_manual_329468.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/intel_qpi_upi_emr_uncore_guide_817509.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/intel_qpi_upi_xeon_scalable_tech_ove ...
====================================================================================================
FAMILY: Chiplet / die-to-die and GPU-scale links: CXL (.cache/.mem link layer + Intel CPI on-die channel interface), UCIe, NVLink/NVLink-C2C/NVSwitc ...
LOAD-BEARING CLAIM: In CXL.cache/.mem ? the flagship open die-to-die protocol of this family ? the provisioned, tuned flow-control resource is explicitly the credit budget rather than buffer bytes: the spec's only sizing rule is a round-trip bandwidth-delay argument on the credit loop ("Credits should be sized to achieve desired levels of bandwidth considering round-trip time of credit return latency"), the budget is a software-set sticky register ("Credits to advertise ... at init", RWS), credits count messages not bytes (one data credit whether the transfer is 32B or 64B), and permission is legally decoupled from storage in both directions (receiver may advertise more credits than the sender's saturating counter can track); moreover Intel's CPI realization of the CXL.cache/mem channels architects precisely DP's structure ? a shared credit pool "shared across virtual channels and protocols" consumed first, plus dedicated per-VC credits as a forward-progress reserve exempt from pool competition, with deadlock freedom hung on fair return of dedicated vs shared credits and on pre-allocated response tranches that are "guaranteed to sink without dependence."
VERDICT: CONFIRMED
CORRECTED: In CXL.cache/.mem ? the flagship open protocol of this family for coherent chip-to-chip and (via CPI/UCIe mappings) die-to-die links ? the provisioned, tuned flow-control resource is explicitly the credit budget rather than buffer bytes: the spec's only flow-control sizing guidance is a round-trip bandwidth-delay argument on the credit loop ("Credits should be sized to achieve desired levels of bandwidth considering round-trip time of credit return latency," explicitly "implementation and usage dependent"), the budget is a software-set sticky register ("Credits to advertise ... at init," attribute RWS = Read-Write-Sticky, ?7.2.2.1.18), credits count messages not bytes ("1 credit allows for one data transfer, regardless of whether the transfer is 64B, 32B or contains Byte Enables"), and permission is legally decoupled from credit accounting on both sides (the receiver may advertise more credits than the sender's required-saturating counter can track, and "it is not required that all credits are accounted for on either side"). Intel's CPI realization of the CXL.cache/mem channels architects this shared-pool-plus-reserve structure: a shared credit pool "shared across virtual channels and protocols" that the spec recommends (not mandates) consuming before dedicated credits, dedicated per-VC credits usable only by "a single VC and protocol combination" and cited as a QoS/forward-progress lever, deadlock avoidance resting on fair return "between dedicated and shared credits," and shared-credit-only operation permitted exactly for VCs "guaranteed to sink without dependence" via pre-allocated response tracking structures (echoing CXL ?3.3.6's pre-allocated NDR/DRS classes).
VERIFY-REASONING: I downloaded both primary specs and verified every quoted phrase verbatim in the extracted text. CXL 1.0 ?4.2.8: "Credits should be sized to achieve desired levels of bandwidth considering round-trip time of credit return latency. This is implementation and usage dependent." ? and this is indeed the spec's only flow-control sizing guidance (buffers merely seed the credit counters: "Each entity should know how many buffers it has and set its credit return counters to these values"; the only other round-trip sizing rule is for the LLR retry buffer, which is retransmission, not flow control, so the claim's "flow-control resource" scoping holds). ?7.2.2.1.18 CXL Link Layer Rx Credit Control Register: fields "Credits to advertise for ... channel at init" with attribute RWS, defined as Read-Write-Sticky in Table 57 ? software-set sticky register confirmed. Credit granularity: "The granularity  ...
DP-RELEVANCE: Strong and direct ? this family is the best-documented domain where DP's "equal permission budget" comparison is physically meaningful. (1) Permission is the architected object: a CXL sender never sees the peer's buffers, only per-class credit counters; the spec sizes credits (not bytes) against the credit-return round trip, exposes the advertised budget as software-writable registers, and legalizes buffer/credit mismatch in both directions ? so holding the credit budget equal while varying its split is exactly the experiment a CXL link architect can run today by reprogramming the Rx Credit Control Register, with physical buffers untouched. (2) The pooled-vs-dedicated split with a progress-guaranteeing reserve is not an invention of DP but the shipped structure of Intel's CPI: a shared credit pool across VCs/protocols consumed first, dedicated per-VC credits as the floor, watermarks against pool monopolization, and mandated fairness between pool and dedicated returns as the anti-deadlock rule ? DP's reserved escape tranche exempt from the pool maps one-to-one onto dedicated credits / pre-allocated response classes ("guaranteed to sink without dependence"). (3) The same counter-vs-storage decoupling recurs at every layer: UCIe retimer credits are advertised counts fronting a privately-sized FIFO, CXL LLR entries and UCIe 8-bit retry counters are RTT-sized windows, and CXL.mem Tags/host snoop-filter entries are tracker permissions that bind system scale independent of link buffering. One honest caveat for the paper: in these protocols the pool is shared across message classes/VCs converging at ONE receiver, whereas DP pools across the two opposing inports of a dimension (two different upstream senders); no fetched source shows pooling across distinct physical inports, so DP should claim the credit-economics analogy (permission scarce, pooled budget + dedicated reserve) rather than an exact topological precedent. Secondary caveat: NVLink evidence is secondary-source only, and AMD IF credit details are NDA'd ? the quantitative case rests on CXL/CPI/UCIe.
----------------------------------------------------------------------------------------------------
  TOKEN: CXL.cache/CXL.mem link-layer channel credits (per message class: D2H/H2D Req, Rsp, Data for .cache; Req, RwD, NDR, DRS for .mem), returned in-band via the flit header's independent Request/Response/Data Credit Return (CRD) fields with exponential encoding 0/1/2/4/8/16/32/64.
  SCARCITY: The spec's only provisioning rule for the budget is a bandwidth-delay argument on the CREDIT loop, not a byte-count rule: credits must cover the credit-return round trip or the link is throughput-bound by permissions. Credit granularity is per message, not per byte (a 32B and a 64B transfer cost the same 1 data credit), so what is being rationed is outstanding-message slots. The CRD field is 3 bits per class per flit (max 64 credits returned per flit per class), i.e., credit-return bandwidth itself is a budgeted resource.
  DECOUPLED: Explicitly decoupled in both directions. Receiver side provisions buffers and derives advertised credits from them ('Each entity should know how many buffers it has and set its credit return counters to these values'); sender side provisions counters, and the spec legalizes counter < buffer mismatch: 'It is possible that the receiver will make available more credits than the sender can track for a given message class. For correct operation, it is therefore required that the credit counters at th ...
  POOLING: In the base CXL link layer there is NO pooling: each message class is credited independently ('Req & RwD message classes, each, need to be credited independently between each hop in a multi-hop fabric... these must eventually drain without dependency on any other traffic type'). The deadlock story is a per-class reserve taken to the extreme: response classes are pre-allocated at the source and thereby exempt from blocking ('NDR & DRS message classes, each, need to be pre-allocated at the source. This guarantees that the responses can sink and ensures forward progress'), while only the request class may block indefinitely (Table 5: D2H Request 'Credited to Host / [blocking] Indefinite' vs Rsp/Data 'Pre-allocated / Link-layer only, must make progress').
  RESERVE: Pre-allocated response classes (NDR/DRS at source; D2H/H2D Rsp+Data 'headed to specified Host/device buffer') are the escape tranche: they are guaranteed sinkable without dependence and must make progress; only Req classes are allowed unbounded blocking. Forward progress is carried entirely by this reserved, dependency-free tranche ? structurally identical to DP's reserved escape tranche.
  QUOTES: {'fetched-fulltext': 7}
----------------------------------------------------------------------------------------------------
  TOKEN: Intel CPI shared credits vs dedicated credits ? the on-die channel interface that carries CXL.cache/CXL.mem between agent and fabric: each physical channel (REQ/DATA/RSP) has a two-tier credit budget: dedicated credits bound to a VC_ID, plus a shared credit pool usable by any VC and any protocol on  ...
  SCARCITY: The shared pool exists precisely because provisioning worst-case dedicated credits per VC is too expensive: ?4.6.4 'Credit Combining Allowances' permits advertising only shared credits for VCs guaranteed to sink 'to avoid the need for dedicated, per VC_ID credits', with the RSP channel as the canonical example when 'a pre-allocated tracking structure that can absorb the response' exists. QoS enforcement is likewise expressed in credit currency, not bytes: give a VC 'enough dedicated credits', or watermark the shared pool.
  DECOUPLED: Credits count messages, not bytes ('Each credit represents a message. Therefore, only a single credit is used to send a 64B payload on the DATA physical channel'; 32B messages also cost one 64B-worth credit). Receiver provisions buffers/trackers; transmitter sees only two counters per channel. An agent may back its RSP credits with a pre-allocated tracker rather than a channel buffer.
  POOLING: This IS the pooled-permission mechanism: 'dedicated credit return wires for dedicated credits (VC ID-based) and shared credits (shared across virtual channels and protocols)'. Transmit policy: 'use shared credits first and only use dedicated credits (per VC credits) after shared credits have been fully consumed' ? i.e., the pool is the working budget and the dedicated tranche is the guaranteed floor. Anti-starvation on the pool is explicit: TX 'watermarks to prevent other VCs from taking significant fraction of shared credits'.
  RESERVE: Two-level story: (1) the dedicated per-VC credits are a per-class reserve exempt from pool competition ? a VC's forward progress never depends on winning shared credits; (2) return-side fairness is mandated: 'To avoid deadlock, a receiver must ensure fairness in returning each dedicated credit type and between dedicated and shared credits.' Pooling is only permitted to fully replace dedicated credits when sink-without-dependence is guaranteed (pre-allocated tracking).
  QUOTES: {'fetched-fulltext': 7}
----------------------------------------------------------------------------------------------------
  TOKEN: CXL transaction-tracking permissions: the CXL.mem 16-bit Tag naming a pre-allocated source tracker entry, and the host snoop-filter/tracker entries that gate how much a CXL.cache device may cache.
  SCARCITY: The survey states outright that the tracker, not bandwidth or buffers, is the scaling limiter: host snoop-filter capacity bounds device caching, the host advertises this limit, and software enforces it at enumeration. This is a permission structure (tracker entries) being the provisioned scarce resource at system scale.
  DECOUPLED: Tracker entries are pre-allocated at the source for the transaction lifetime regardless of link buffer occupancy; a device can have ample link credits and still be throttled by host tracker sizing.
  POOLING: CacheID partitions (rather than pools) tracker capacity per device to avoid degrading to multicast snooping; the host-advertised limit is a budget handed out across devices. No dynamic pooling mechanism is specified ? capacity is statically split, which is exactly the 'fixed split' baseline DP compares against.
  RESERVE: 
  QUOTES: {'fetched-fulltext': 3}
----------------------------------------------------------------------------------------------------
  TOKEN: CXL Link Layer Retry buffer entries (LLRB) with implicit sequence numbers ? the per-link window of retryable flits in flight, a second permission-like budget independent of channel credits.
  SCARCITY: The spec gives an explicit bandwidth-delay sizing law for LLRB entries: undersize the entry count and the sender stalls waiting for Acks even with credits in hand. This is the retry-window analog of the credit-RTT rule and shows every die-to-die boundary carries TWO token loops (credit loop + Ack loop), each provisioned as a counter/entry budget against the same round trip.
  DECOUPLED: LLRB is sender storage, but the binding quantity is entries-versus-RTT, and the Ack return (the permission to free an entry) is what gates injection; ACK forcing exists because permission return, not storage, is the critical resource.
  POOLING: None ? one LLRB per link direction, shared by all message classes of both protocols multiplexed on the link (so it is a de facto pooled budget across .cache and .mem traffic, with no per-class reserve; forward progress relies on the Ack loop, not on class separation).
  RESERVE: 
  QUOTES: {'fetched-fulltext': 2}
----------------------------------------------------------------------------------------------------
  TOKEN: UCIe flit-level credits: the flit-header Credit field (transaction-layer credit management), the retimer receive-buffer credits advertised at parameter exchange, and the simplified retry scheme's 8-bit counters.
  SCARCITY: Flit-header real estate for credits competes directly with payload (16B of a 256B flit carries 'CRC, FEC, Credits, etc.'), so the credit-return channel is itself an efficiency-budgeted resource. Per the Intel UCIe patent family (search-rendered), a die may not send ANY flit ? even a null flit ? without a credit, and credit counters are reset to the advertised value on state transitions: the counter is the interface contract, the FIFO behind it is private to the retimer.
  DECOUPLED: Explicit: the retimer owns and sizes the Rx buffer; the die sees only 'the number of credits available for the retimer receive buffer' advertised over sideband. One credit = one 256B flit regardless of content (patent, search-rendered). The Tx retry buffer is storage the sender provisions, while the Ack loop returns the permission.
  POOLING: No credit pooling in the adapter; the pooled-vs-dedicated question shows up instead as link bandwidth multiplexing across protocol stacks: with enhanced multi_protocol_enable 'the Adapter must advertise the maximum percentage of bandwidth that the receiver for each protocol can accept' ? a floating split of link capacity with per-stack advertised caps (a watermark-style reserve), while Raw mode / regular mode is the fixed 50/50 split. Forward progress across the retimer relies on the rule that data sent to a retimer must be consumable by the remote die without other dependencies.
  RESERVE: 
  QUOTES: {'fetched-fulltext': 2, 'secondary-source': 3, 'search-rendered': 1}
----------------------------------------------------------------------------------------------------
  TOKEN: NVLink / NVLink-C2C link-layer credits and NVSwitch per-port transaction tracking. NVIDIA does not publish an NVLink spec; the credit mechanism is attested only via secondary sources, while NVIDIA's own Hot Chips slides and whitepaper attest tracker structures, SRAM buffering, replay, and configurab ...
  SCARCITY: Direct public evidence that credits (vs bytes) are the tuned quantity is NOT available for NVLink ? the strongest primary signals are (a) buffer ALLOCATION (partitioning policy, i.e., how the budget is split) being called out as configurable on a die where >50% of area is PHY and packet logic is 'extremely compact', and (b) CRC+replay tracking per external link implying a per-link outstanding-flit window. The per-flit credit claim itself is secondary.
  DECOUPLED: Not publicly establishable for NVLink; the C2C flit (8B header per 128B flit, secondary) carries credit returns in-band as in CXL, suggesting the same counter-proxies-buffer structure, but this is inference.
  POOLING: 'Configurable buffer allocations' (HC30) implies a partitionable shared SRAM budget across ports/classes inside NVSwitch, but NVIDIA publishes no pooled-vs-dedicated split or its forward-progress rules. No public deadlock-reserve story.
  RESERVE: 
  QUOTES: {'secondary-source': 1, 'fetched-fulltext': 3}
----------------------------------------------------------------------------------------------------
  TOKEN: AMD Infinity Fabric (Scalable Data Fabric, SDF) flow-control credits per traffic class (Request / Probe / Response). Public evidence is architectural only: the coherent-HyperTransport-derived SDF and its message classes are documented; per-class credit counters are industry folklore consistent with  ...
  SCARCITY: Public slides argue in bandwidth-provisioning terms ('171GB/s peak bisection BW; ~2x required ? Overprovisioned for performance scaling') rather than credit terms; the probe filter is called out as 'SRAM-based for minimum indirection latency', i.e., tracker structures are the latency/area-critical resource named in public material. No public statement that credits are the tuned quantity ? this gap should be reported as such.
  DECOUPLED: Not publicly establishable.
  POOLING: Not publicly documented. The visible structure (separate Request/Probe/Response classes over shared die-to-die GMI/xGMI links) implies per-class budgets on a shared physical link, but pooled-vs-dedicated split is not verifiable from public sources.
  RESERVE: 
  QUOTES: {'fetched-fulltext': 3, 'unverified': 1}
SAVED: D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/chiplet_cxl10_spec.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/chiplet_intel_cpi_spec_rev1p0.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/chiplet_cxl_intro_arxiv_2306_11227.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/chiplet_ucie_onpkg_arxiv.txt; D:/THU/sophomore_spring/AI_X/_fukas ...
====================================================================================================
FAMILY: On-chip outstanding-transaction structures: MSHRs (cache miss-status holding registers), coherence directory / home-node trackers (Intel HA  ...
LOAD-BEARING CLAIM: Arm's shipping CMN-700 home node (HN-F) enforces exactly DP's resource model on its coherence tracker: the POCQ is a single fixed-size pool of expensive tracker entries (16-128, '> 64 depths may have frequency implications') whose split across request classes is set by configuration registers into per-class DEDICATED entries ('reserved entries for a given class... not retried... unavailable for other classes to use' ? a reserve exempt from the pool that alone guarantees acceptance), a floating shared region, and per-class max_allowed caps, with a credited-retry (RetryAck/PCrdGrant Protocol Credit) mechanism whose grants are backed by pre-reserved slots ? so 'equal permission budget, floating vs fixed split across flows, with a deadlock-free reserved tranche' is not an artificial comparison but the literal, documented configuration space of a commercial interconnect's tracker.
VERDICT: CONFIRMED
CORRECTED: Arm's CMN-700 HN-F documents, in TRM Sec 5.5 (pp.1192-1198), a tracker resource model matching the comparison's configuration space: the POCQ is a single fixed-size pool of tracker entries (configurable 16-128, default 32; '> 64 depths may have frequency implications'), split by configuration registers into per-class DEDICATED entries ('reserved entries for a given class... not retried... unavailable for other classes to use' ? a per-class reserve, carved out of the pool and exempt from sharing, that alone guarantees acceptance of a first-issue request), a floating non-reserved shared region drawn on once a class exceeds its dedicated count, and per-class max_allowed caps (plus a contended_min priority tier and SF-Evict reserve the comparison omits). Retried requests receive CHI RetryAck/PCrdGrant Protocol Credits granted only when a resource is available, and a credited ('static') request 'cannot be retried so it is allocated in POCQ' ? i.e. grants are acceptance-guaranteed (slot-backed by necessary implication in the TRM, and literally slot-reserved in gem5's CHI-mem.sm via incrementReserved before PCrdGrant). 'Equal permission budget, floating vs fixed split across flows, with a reserved tranche guaranteeing acceptance' is therefore the documented configuration space of a commercial interconnect's tracker ? with the caveat that the TRM frames the dedicated tranche as a no-retry/starvation-freedom guarantee, not in the words 'deadlock-free'.
VERIFY-REASONING: I independently re-extracted every load-bearing passage from the source PDFs on disk (pdftotext on cmn700_trm.pdf and learn_chi.pdf), not just the researcher's .txt extracts, and the PDFs are the genuine Arm publications (correct doc IDs 102308_0302_07_en / 102407_0102_01_en in footers, revision history, 1306 pp.). Every quoted element checks out verbatim: (1) NUM_ENTRIES_POCQ = 16/24/32/48/64/80/96/128 (default 32) with the exact comment '> 64 depths may have frequency implications' (TRM p.31-32); (2) Sec 5.5 'HNF class-based resource allocation and arbitration' at pp.1192-1198 as cited; (3) cmn_hns_pocq_alloc_class_dedicated: 'Dedicated entries are reserved entries for a given class in POCQ... any incoming request to that class is not retried. Unused dedicated entries belonging to a class are unavailable for other classes to use' ? the ellipsized quote is faithful; (4) per-class max_al ...
DP-RELEVANCE: This family is the strongest possible grounding for DP's 'equal permission budget' methodology, because here the permission token is unambiguously the expensive resource and is ALREADY managed as a poolable budget with reserved tranches in shipping hardware. (1) Token != buffer, by construction: an MSHR/TBE/TOR/RPQ-credit is a tracker entry or counter physically and administratively separate from data storage ? gem5 provisions cache size and mshrs as independent parameters; Intel's HA holds credit counters for queue storage that lives in a different unit (iMC); CHI types its Protocol Credits per backing resource. A practitioner in this domain natively thinks in 'entries/credits', not KB of buffering. (2) The token is the scarce, binding constraint: counts are 4-128 while attached storage is KB-MB; Intel documents the tracker as 'the critical resource' and ships PMU events specifically for token starvation (TOR occupancy, no-RTID retries, RPQ-credits-empty cycles); Arm documents that deepening the POCQ tracker beyond 64 'may have frequency implications'. Comparing designs at equal tracker-entry budget is therefore the standard fair-cost comparison here ? it is what 'iso-resource' means in this literature. (3) DP's exact mechanism has three shipping/reference precedents at three granularities: gem5's MSHR queue (one pooled file, demand_mshr_reserve tranche exempt so the deniable class (prefetch) cannot starve the critical class), Intel's TOR/RPQ (shared pool + 'reserved TOR entries for various request types', regular vs reserved-ISOCH credits), and ? most precisely ? CMN-700's POCQ class allocation, where ONE fixed entry budget is split by config registers into per-class dedicated (reserved, non-poolable, guaranteeing no-retry acceptance = forward progress) + floating shared region + per-class caps, with credited retry whose grants are slot-backed. DP's 'joint budget S over a dimension's two opposing inports, enforced only by a counter, with a pool-exempt escape tranche carrying deadlock freedom' is structurally the same design, transplanted from a home-node tracker onto a torus router's dimension pair; a memory-system architect would recognize 'floating vs fixed split at equal token budget' as exactly the dedicated/shared/max_allowed tuning question CMN-700 exposes. Caveats for honest citation: the classic academic MSHR-scaling quotes (Kroft 1981; Tuck et al. MICRO'06 CAM-cost argument) could not be fetched this session ? WebFetch's domain-verification backend was down for all hosts and the search budget was exhausted ? so those specific claims are labeled search-rendered/unverified and should be re-fetched (URLs provided) before appearing verbatim in the paper; all load-bearing mechanism claims above rest on fetched-fulltext vendor documentation and gem5 source instead.
----------------------------------------------------------------------------------------------------
  TOKEN: MSHR entry (miss-status holding register) ? the permission for a cache to have one more outstanding miss. In gem5 the parameter is literally named 'mshrs = Number of MSHRs (max outstanding requests)'. Industry L1 equivalents: Intel line-fill buffers, AMD miss-address buffers.
  SCARCITY: Counts are tiny compared to storage: gem5's shipped defaults are mshrs=4 for L1 and mshrs=20 for L2 (configs/common/Caches.py), i.e. a 32KB L1 with 512 lines of storage gets only 4 outstanding-permissions, and exhausting them blocks the ENTIRE cache: base.hh allocateMissBuffer() calls setBlocked(Blocked_NoMSHRs) the moment mshrQueue.isFull(). The binding-constraint framing (outstanding = BW x latency, so few MSHRs cap bandwidth regardless of cache size) is standard; the existence of a research line on scaling them (Tuck et al. MICRO'06 'Scalable Cache Miss Handling for High Memory-Level Parallelism'; Asiatici & Ienne FPGA'19 'Stop Crying Over Your Cache Miss Rate: Handling Efficiently Thousa ...
  DECOUPLED: Yes, fully. gem5 provisions size (data storage), mshrs (outstanding permissions), tgts_per_mshr (merge capacity per permission), and write_buffers as four independent parameters; the MSHR 'keeps all the information needed to handle a cache miss including a list of target requests' but holds no data ? the response fills the cache block directly. A design can have thousands of lines of buffer and 4 permissions (the default!), or (via tgts_per_mshr) many logical requests riding one permission. Cach ...
  POOLING: The MSHR file is ONE pooled budget shared by all addresses and by two competing flows: demand misses and prefetches. gem5 implements exactly a floating pool with a reserved tranche exempt from the sharing: demandReserve ('The number of entries to reserve for future demand accesses. Prevent prefetcher from taking all mshr entries') makes canPrefetch() deny prefetch allocation once allocated >= numEntries - (numReserve + 1 + demandReserve), while demands may consume everything down to the reserve. Forward-progress story: when the pool is exhausted the cache back-pressures (setBlocked) rather than dropping ? safe stall, no deadlock ? and the reserved tranche guarantees the latency-critical class (demands) can always make progress even under prefetch flood.
  RESERVE: demand_mshr_reserve = Param.Unsigned(1, 'MSHRs reserved for demand access') ? a per-class reserved tranche exempt from the shared pool, carrying forward progress for demand misses; prefetches are the poolable, deniable class. Plus numReserve ('minimum number of entries needed to satisfy any access') held back so an in-flight access can always complete.
  QUOTES: {'fetched-fulltext': 6, 'search-rendered': 2, 'unverified': 1}
----------------------------------------------------------------------------------------------------
  TOKEN: Home-node / directory tracker entry: Intel Home Agent 'Tracker File' (TF) entry and CHA/CBo 'TOR' (Table Of Requests) entry, indexed by a source-held RTID (Request Transaction ID) in QPI-era parts; Arm CHI TBE (Transaction Buffer Entry) at HN-F, physically the CMN-700 POCQ (Point-of-Coherency Queue) ...
  SCARCITY: Intel's own PMU manual calls the tracker the binding resource: 'TF is the critical resource each transaction needs before being sent to the iMC' (128 entries serving a whole socket), and for the LLC slice: 'The TOR is more commonly a bottleneck in skews with smaller core counts, where the ratio of RTIDs to TOR entries is larger.' A dedicated retry event exists solely for token exhaustion ('retried because there were no RTIDs available'). On the Arm side, CMN-700's config table warns that scaling the tracker costs frequency: POCQ depth options 16-128 with '> 64 depths may have frequency implications' ? direct evidence the tracker entry is the expensive hardware, kept small while SLC storage s ...
  DECOUPLED: Yes, and doubly so: (1) the permission is held by a DIFFERENT agent than the storage ? a requester holds an RTID / Protocol Credit while the tracker entry and data buffer live at the home node; CHI even types the credit per backing resource: 'Read Requests and Write Requests can use separate Data Buffers, so each buffer can use a different type of Protocol Credit to indicate availability. The values for different types of Protocol Credit are defined by the implementation.' (2) gem5's TBEStorage  ...
  POOLING: Richest pooling story in the family. (a) Arm CMN-700 HN-F allocates its ONE POCQ pool by class via three registers: dedicated (reserved entries unusable by other classes), max_allowed (per-class cap 'even if POCQ has available entries'), contended_min (priority floor for credit-grant arbitration under contention) ? i.e., a fixed total tracker budget whose split between flows is a configuration choice ranging from fully floating to fully partitioned. (b) Intel TOR: 'there are reserved TOR entries for various request types, so it is possible that a given request type be blocked with an occupancy that is less than 20' ? shared pool with per-type reserved tranches; RTIDs come in separate local/remote pools ('multiple RTID pools for the different sockets'). (c) CHI Request Retry: on exhaustion the completer sends RetryAck, then PCrdGrant when a slot frees; gem5's SN implements it by RESERVING ...
  RESERVE: Explicit and per-class: CMN-700 'Dedicated entries are reserved entries for a given class in POCQ... any incoming request to that class is not retried. Unused dedicated entries belonging to a class are unavailable for other classes to use' (plus an SF-eviction entry reserved at slot 0); Intel TOR's per-request-type reserved entries; gem5 CHI's reserved-slot-before-PCrdGrant discipline. In every case a reserved tranche outside the shared pool carries forward progress, exactly DP's escape-tranche  ...
  QUOTES: {'fetched-fulltext': 16}
----------------------------------------------------------------------------------------------------
  TOKEN: Memory-controller pending-queue entry, consumed remotely as an 'iMC RPQ/WPQ credit' held by the Home Agent (Intel), or locally as a read/write queue entry (gem5 MemCtrl read_buffer_size / write_buffer_size).
  SCARCITY: Intel dedicates PMU events purely to token starvation: the HA section's stated purpose for iMC RPQ/WPQ events is to 'Determine cycles the HA is stuck without credits in to the iMCs read/write queues', with events RPQ_CYCLES_NO_REG_CREDITS ('iMC RPQ Credits Empty - Regular') and WPQ_CYCLES_NO_REG_CREDITS. Entry counts are tens (gem5 defaults: 32 read / 64 write entries; HBM config 64/128), far below the storage the DRAM devices themselves represent ? the queue entry, and the FR-FCFS scheduling CAM search over it, is what stays small. A whole-socket 128-entry HA tracker feeding per-channel few-tens-entry queues means the credit, not DRAM bandwidth, is frequently the observed stall reason (henc ...
  DECOUPLED: Yes ? the cleanest holder/owner split in the family: the credit counter lives in the Home Agent (the consumer of the permission) while the queue storage lives in the iMC; Intel provisions them independently per channel, and the HA must 'first acquire a credit' before any read is even sent. In gem5 the buffer counts are parameters of the memory interface, independent of the sizes of every upstream cache/queue that may hold the data.
  POOLING: The RPQ credit pool is split into a general floating pool plus a reserved tranche for a protected traffic class: 'This queue is broken into regular credits/buffers that are used by general reads, and "special" requests such as ISOCH reads.' Regular high-bandwidth traffic can exhaust all regular credits but cannot touch the special/ISOCH buffers ? a per-class reserve exempt from the shared pool that guarantees isochronous forward progress (real-time memory QoS). gem5 mirrors class-partitioning with per-QoS-priority MemPacketQueues, and write-drain hysteresis (writes accumulate until a threshold, then reads are stopped) is budget arbitration between the two directions' token pools.
  RESERVE: The 'special'/ISOCH credit tranche: exempt from the regular pool, reserved so isochronous requests always find an entry ('it will be difficult (and uncommon) to make use of both the regular and special buffers at the same time'). Everything else relies on backpressure-stall (readQueueFull -> retry to requester), which is safe because the DRAM controller is a sink that always drains.
  QUOTES: {'fetched-fulltext': 5}
SAVED: D:\THU\sophomore_spring\AI_X\_fukase_tmp\permission_ifaces\mshr_gem5_mshr_queue_hh.txt; D:\THU\sophomore_spring\AI_X\_fukase_tmp\permission_ifaces\mshr_gem5_Cache_py.txt; D:\THU\sophomore_spring\AI_X\_fukase_tmp\permission_ifaces\mshr_gem5_Caches_defaults_py.txt; D:\THU\sophomore_spring\AI_X\_fukase_tmp\permission_ifaces\mshr_gem5_cache_base_hh.txt; D:\THU\sophomore_spring\AI_X\_fukase_tmp\permiss ...
====================================================================================================
FAMILY: Supercomputer/data-center fabrics with credit-based link flow control: InfiniBand (per-VL FCCL/FCTBS credits), Intel Omni-Path (per-VL credi ...
LOAD-BEARING CLAIM: In IB-lineage HPC fabrics the transmission permission is literally a counter, not the buffer: Intel Omni-Path's credit architecture (US9385962) has the receiver expose ONE undivided physical buffer pool while transmitter-side credit registers split the same total budget into per-VL DEDICATED reserves plus a dynamically SHARED pool ? consumed dedicated-first, per-VL-capped against starvation, and with the fixed-vs-floating ratio retunable at runtime by the Fabric Manager at constant total ? so "equal total credit budget, floating vs fixed split across flows, with a dedicated reserve exempt from the pool carrying forward progress" is an already-institutionalized design axis, not a construct invented for DP; and the token itself is demonstrably the binding resource in this domain, since InfiniBand's 12-bit/2048-credit-per-VL field caps usable link length via the bandwidth-delay product (~1 km at 100 Gb/s per Mellanox US9584429) independent of how much SRAM one could afford.
VERDICT: CONFIRMED
CORRECTED: In IB-lineage HPC fabrics the transmission permission is literally a transmitter-side counter: Intel's flexible-credit-exchange patent US9385962 (the architecture productized as Omni-Path, though the patent predates the brand name) makes credit management "the responsibility of the transmitter," splitting the receiver's fixed total buffer budget ? by Fabric-Manager configuration, not fixed hardware ? into per-VL DEDICATED reserves (Dedicated_Limit[VL]) plus a SHARED pool (Global_Shared_Limit), consumed dedicated-first, per-VL-capped (Shared_Usage_Limit[VL]) explicitly to prevent one VL from "starving shared buffer space by other VLs," and retunable by the Fabric Manager at runtime without quiescing the link or coordinating with the receiver, under the invariant that dedicated limits plus the shared limit cannot exceed the fixed total. (Precision caveats: the patent describes the receive buffer as partitioned into per-VL buffers plus shared pool(s) ? a reconfigurable logical organization of one buffer budget, not literally "one undivided physical pool"; and dedicated+shared is one of three offered organizations, dedicated-only included.) So "equal total credit budget, floating vs fixed split across flows, with a dedicated reserve exempt from the pool" is indeed an institutionalized design axis predating DP. And the token is demonstrably the binding resource in this domain: Mellanox US9584429 states that InfiniBand's 12-bit flow-control fields cap credits at 2^11 = 2048 ? 64 B = 128 KB per VL, which by the bandwidth-delay product (L < BSIZE?V/2BR) limits link length to about 1.05 km at 100 Gb/s regardless of how much buffer memory is installed ? the exact problem that patent exists to work around.
VERIFY-REASONING: I fetched the actual full text of the two load-bearing sources (freepatentsonline via curl, since the WebFetch tool blocked patent hosts) plus the Mellanox IEEE 802.1 tutorial PDF (extracted with pdftotext), and checked each mechanism. VERIFIED VERBATIM in Intel US9385962 ("Method and system for flexible credit exchange within high performance fabrics", assignee INTEL CORPORATION, filed 2013-12-20): (1) Transmitter-side counter as the permission: "the architecture is able to make credit management and tracking the responsibility of the transmitter"; "Allows management of credit allocation to be performed solely by the transmitter." (2) Per-VL dedicated + shared pool in transmitter-side registers: TABLE 4 registers Dedicated_Limit[VL], Global_Shared_Limit, Shared_Usage_Limit[VL], "all in units of the peer's Allocation Unit"; "The sum of the dedicated limits and global shared limit cannot  ...
DP-RELEVANCE: STRONGLY RELEVANT, with one precise caveat. (1) Token = counter, decoupled from storage: IB link FC is enforced purely by 12-bit counter arithmetic (FCTBS+size<=FCCL), and Omni-Path proves the partition of a credit budget is transmitter-side counter state over a single undivided receive buffer ? exactly DP's premise that the split (fixed or floating) lives in the permission ledger, not in the SRAM. The Cisco US12375431 patent shows practitioners even MOVE capacity by decrementing credit counters first and touching physical buffers second. (2) Token scarcity is real and historically binding: the 2048-credit/128KB-per-VL ceiling from the 12-bit credit fields ? not buffer cost, not wire ? is what limits IB link length (L<BSIZE*V/(2*BR); ~1.05 km at 100G), and the industry response was to convert/widen credit units (US9584429) or abandon credits entirely for long haul (US7952998): clear precedent that the permission budget can bind before storage does, which is the exact regime where DP's "equal permission budget" comparison is the physically fair one. (3) The comparison axis is recognized verbatim: OPA fabric managers literally retune "the relative allocation of credits between various VLs during runtime operations" at fixed total ? i.e., same total credits, dedicated (fixed) vs shared (floating) split, with consumption order dedicated-first and a per-class starvation cap; base InfiniBand (mandatory per-VL separation, VL15 reserve) is the pure fixed-split endpoint of the same axis. A fabric architect would immediately parse DP's "equal budget S, floating vs static split, reserved escape tranche exempt from the pool" as the OPA Dedicated_Limit/Global_Shared_Limit/Shared_Limit pattern applied to a router-inport pair. (4) The specific PAIR granularity (two opposing inports of one dimension) has direct academic precedent: DAMQS pools the buffer of VCs from two different physical channels of one router (east-X+south-Y), with two slots per VC reserved before sharing ? reserve-plus-pool across a channel pair. CAVEAT to state honestly in the paper: in these fabrics pooled credits are receiver-backed, so pooling credits usually implies pooling the backing store (OPA single pool, DAMQ shared SRAM); DP's variant ? pooled counter over buffers that stay PHYSICALLY private per inport ? is the transmit-side/ledger half of the OPA pattern without merging the SRAM, i.e., it trades away DAMQ's storage-utilization gain to keep single-ported private buffers, and its "equal token budget" comparison matches how this domain provisions (FM-managed credit budgets) rather than how it floorplans SRAM. Also note: pooling across the two DIRECTIONS of one cable is NOT a standard axis at link level (each direction's credits govern an independent receiver); the recognized axes are across VLs/classes at one receiver and across inport pairs inside a router (DAMQS) ? DP should claim kinship with the latter two, not with cable-direction pairing.
----------------------------------------------------------------------------------------------------
  TOKEN: InfiniBand per-Virtual-Lane link-layer flow-control credit: FCCL (Flow Control Credit Limit) granted by the receiver, gated at the transmitter against FCTBS (total blocks sent); 1 credit = one 64B block. Spec: IBTA InfiniBand Architecture Specification Vol.1 Section 7.9 ? the spec itself is MEMBER-O ...
  SCARCITY: Three independent lines: (1) The token namespace itself is capped ? 12-bit fields give max 2048 credits = 128KB per VL, and this cap (not wire quality) limits usable link length via the bandwidth-delay product: a Mellanox patent derives L < BSIZE*V/(2*BR) and computes that a 128KB receive buffer at 100 Gb/s caps the link at ~1.05 km. (2) IBM patented CREDIT-LESS flow control for long-distance IB precisely because the credit exchange, not bandwidth, was the binding constraint. (3) The credit mechanism exists to economize buffers: Mellanox markets that lossless credit FC is why IB switches need significantly less buffer memory than competing QoS technologies ? i.e., credits are the cheap resou ...
  DECOUPLED: Partially, and in the direction DP needs: FCCL is receiver-computed as ABR + available buffer, so a receiver may back credits with any amount of storage it chooses (fewer tokens than buffer is trivially legal); the converse binds ? a receiver with MORE than 128KB/VL of SRAM cannot advertise it because the 12-bit token field runs out first, which is why Mellanox patented converting credit counts between block sizes (US9584429) and IBM patented ignoring credits altogether (US7952998). Receiver pro ...
  POOLING: Base InfiniBand explicitly FORBIDS pooling at the link level: credits are per-VL and the spec mandates separate receive buffering resources per VL, precisely to prevent HOL blocking between classes; VL15 (management) has its own always-separate resources ? a hard per-class reserve. So IB itself is the 'fixed split' endpoint of DP's comparison axis, with the deadlock/QoS story carried entirely by mandatory per-class dedication. The 'floating' endpoint appears in IB's descendants (Omni-Path, next token) and in patched IB gear (Cisco buffer-trading patent, token 3).
  RESERVE: Per-VL separation IS the reserve: 'InfiniBand Mandates Separate Receive Buffering Resources per VL' (tutorial), minimum buffering of one packet / credit for one packet per data VL, and a dedicated management VL (VL15) that never competes with data VLs.
  QUOTES: {'fetched-fulltext': 8, 'search-rendered': 1}
----------------------------------------------------------------------------------------------------
  TOKEN: Intel Omni-Path per-VL flow-control credits over a DEDICATED + SHARED credit pool: Dedicated_Limit[VL] (per-VL reserved credits), Global_Shared_Limit (pooled credits usable by any VL), Shared_Limit[VL] (per-VL cap on pool usage), all in Allocation Units; credit returns ride LTP sideband bits. (HotI' ...
  SCARCITY: Credit availability is the sole transmit gate ('for a fabric port to send a flit it needs sufficient flow control credits available for the required buffer space at the receiving port'), and the Fabric Manager treats the credit allocation ? not the SRAM ? as the runtime-managed commodity: 'a centralized Fabric Manager may make changes to the relative allocation of credits between various VLs during runtime operations' at constant total buffer (sum of dedicated limits + global shared limit cannot exceed total buffer space).
  DECOUPLED: Yes, explicitly and by design: 'receivers provide a single pool of receive buffers for the Virtual Lanes (VLs) supported on a link. The allocation of the buffer pool is managed by logic on the transmitter side of the link.' Storage is provisioned once by the receiver; the token partition (fixed vs floating) is provisioned by software (Fabric Manager) writing transmitter registers, retunable live.
  POOLING: The definitive industrial precedent for DP: per-VL dedicated credits (reserve tranche) + a global shared credit pool + per-VL caps on pool consumption. Consumption order is dedicated-first-then-shared; replenishment is shared-first-then-dedicated (identical bookkeeping shape to DP's escape-tranche-first accounting). Extended to 3-tier (per-VL, per-VL-subset pool, global pool). Forward-progress story: Intel recommends the FM 'configure some dedicated space for each active VL' for QoS separation ? a per-class reserve exempt from the pool ? and Shared_Limit[VL] prevents one class from starving others out of the pooled tranche.
  RESERVE: Dedicated_Limit[VL] is exactly a per-class reserve exempt from the shared pool: 'To ensure QoS separation between VLs, it is recommended that the Fabric Manager configure some dedicated space for each active VL.' Anti-starvation cap on the pooled tranche: Shared_Limit[VL] prevents 'congestion or bursts of traffic on a single VL from starving shared buffer space by other VLs.'
  QUOTES: {'fetched-fulltext': 6}
----------------------------------------------------------------------------------------------------
  TOKEN: Runtime-tradable transmit credit counter in credited (lossless) switch fabrics: live reallocation of buffers 'between ports, or virtual lanes of a port' is executed THROUGH the credit counter ? decrement the peer's transmit credits first, physical buffers move second (Cisco, 2025).
  SCARCITY: Receive buffers/credits are scarce enough that a 2024-filed, 2025-granted patent exists solely for moving them between ports and VLs of a live switch without link reset or frame loss ? nobody patents non-disruptive trading of an abundant resource.
  DECOUPLED: Demonstrates the ordering DP assumes: permission (credit count) is retracted first and independently; storage is released only after the counter decrement is confirmed ? counter state, not SRAM occupancy, is the safety invariant.
  POOLING: Explicit cross-port AND cross-VL budget reallocation on a running credited network; the credit decrement/confirm handshake is what keeps the transition loss-free (forward progress preserved because the peer can never hold more permission than backing store at any instant).
  RESERVE: 
  QUOTES: {'fetched-fulltext': 2}
----------------------------------------------------------------------------------------------------
  TOKEN: Academic DAMQ-lineage shared buffer-slot grants across VCs of DIFFERENT physical channels with per-VC reserved slots (DAMQS/DAMQAS); plus, in Slingshot, NIC request-queue credits repurposed as an occupancy/congestion estimator for adaptive routing.
  SCARCITY: The DAMQ tradition exists because statically split buffers waste the scarce resource: 'As virtual channels are not equally used in many applications, if they share a common buffer, the whole buffer space will be better utilized' ? i.e., at fixed total budget, the floating split beats the fixed split, which is precisely DP's experimental claim, established here for the storage-backed version of the token.
  DECOUPLED: In DAMQ the pooled commodity is the storage itself (slots), with the per-VC ledger (head/tail pointers, reserved-slot counters) as bookkeeping ? the mirror image of DP, which pools the ledger and keeps storage private. The pair brackets the design space: OPA pools counters over one buffer; DAMQ pools one buffer under per-VC counters; DP pools counters over PRIVATE buffers.
  POOLING: Directly on DP's axis including the DIRECTION dimension: 'DAMQS buffer combines the buffer for virtual channels from two different physical channels' (east-X with south-Y, west-X with north-Y) ? a joint budget across two inports of a router, the same granularity as DP's two opposing inports of one dimension. Forward-progress story is a per-VC reserve exempt from sharing: two slots per VC are held back before any sharing occurs, so no VC can be fully starved by the pool.
  RESERVE: Per-VC fixed reservation exempt from the shared pool: 'Two buffer slots are reserved for each virtual channel before the buffer accepts any incoming flit' ? the same role as DP's escape tranche (guarantees each class can always make progress regardless of pool occupancy).
  QUOTES: {'fetched-fulltext': 4, 'unverified': 1}
SAVED: D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/infiniband_ieee802_crupnicoff_tutorial.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/ib_credits_tutorial.pdf; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/omnipath_us9385962_flexible_credit.txt; D:/THU/sophomore_spring/AI_X/_fukase_tmp/permission_ifaces/infiniband_us9584429_longhaul_credit.txt; D:/THU/sophom ...