# DP-Phys：对向输入端口物理槽位共享（变体A）设计

**状态**：讨论稿——只定义与设计，不含实现。
**日期**：2026-09-02
**脉络**：DP 第三阶段。阶段1（`naive-research/`）在等承诺约定下测了许可共享；
阶段2（`research/`，即原 physical_bound_research）证明等物理存储下许可计数器
零收益（1700/1700，见其 README §6）。本文给出阶段2指向的修正机制：**共享存储
本身**，让计数器永远推不动的落地上限真正动起来。

声明纪律沿用 `permission_domain/design.md` §0：外部论文的机制性陈述一律标
**[已验证: 出处]** 或 **[未验证]**；自己的代码与数据注明文件。

---

## 1. 机制

固定一个路由器和一个维度，取对向输入端口对 `{E-in, W-in}`（N/S 对、第三维
上下对同理）。

**不变的**：每侧仍有 `v` 条 VC，VC 编号域与每侧 `r` 个保留 credit 的语义与
私有基线相同。
**新增的**：池 credit 是一个**动态的、上游可见的**信号——credit 流控无
拒收（flit 上线不可退回），"池满勿发"的门只能设在上游，而池余量取决于对向
的实时占用，故两个上游看到的池 credit 必然此消彼长地动态调整（详见 §5.4；
工业先例 UPI VNA/VN0 同构）。

**变的**：这对端口共用一份物理槽预算 `B_pair`。每侧私有 **保留区** `r` 个槽
（死锁基座），其余 `P = B_pair − 2r` 个槽构成 **共享池**，两侧的 flit 都可落入。

**用户表述的机械化**："E 来的包多了，W 的 VC 被堵住"——E 的 flit 真实占据池
槽后，W 的 VC 编号还在、credit 还在发，但超出保留区的部分**收不进 flit**：W 的
可用容量随 E 的驻留量实时缩水。这是对真实存储的争用，不是记账假象。

---

## 2. 形式定义

**定义 1（池）**：对 `{f, f′}` 与 vnet `k`：`occ_f(t)` 为 `f` 侧物理驻留的
flit 数；**池占用** `u_f(t) = max(0, occ_f(t) − r)`（前 `r` 个驻留 flit 记在
保留区，豁免）。

**不变式 PHY**：`u_f(t) + u_{f′}(t) ≤ P`，恒成立。

**定义 2（动态落地上限）**：
```
H_f(t) = r + (P − u_{f′}(t))      ∈ [r, r+P]
```
私有基线是常数 `H_f = r + P/2`——阶段2 Corollary 1 证明任何计数器都推不动
这个量；变体A 靠移动 flit（而非数字）推动它。对向空闲时增益 `ΔH = P/2`，
硬件总量不变。

**注（VC 并行度与容量解耦）**：`v` 不再钳制驻留量——VC 变"深"（固定编号背后
是槽池），一条 VC 可排多个 1-flit 包。`v` 只限制分配器的并发 VC 分配数，即
并行度；容量由槽承载。编号载并行、槽载容量，这个分离是链路不动而上限能抬的
原因。

**定义 3（方向 cap）**：`c ∈ [⌈P/2⌉, P]`，强制 `u_f ≤ c`。
`c = P`：无治理，最大增益、最大侵占暴露；
`c = P/2`：每侧保底私有份额，零干扰退化、零借用增益。

**定义 4（floor）**：`floor_f(c) = r + (P − c)`——对向侵占到顶时 `f` 仍保有的
容量。阶段1/2 的 cap 管的是没有实体的计数器（"对着空气限流"）；这里同一套
纪律管真实槽位，防的是经典 DAMQ 饥饿。**cap 第一次有了能赢的岗位**——阶段2
已证明当池是虚构时它只能输。

**定义 5（死锁基座隔离）**：逃逸 VC（routing 4）的 flit 只映射到保留区槽，
永不入池。逃逸网络的前向进展证明只读保留区状态，而不变式 PHY 使保留区独立于
池争用：**基线的死锁自由证明逐字迁移**。（刻意不用 DP-CBS 基座：阶段2 §4.2
显示其子环重构在 xopposite 上独立于 cap 亏 −22%，属基座伪影，换基座排除。）

---

## 3. 成立条件（何时有收益）

**C1（缓冲为瓶颈）**：`BDP ≈ RTT_credit × BW_link > r + P/2`。
抬高上限只有在缓冲本身限制吞吐时才变成吞吐。C1 不满足时（私有份额已覆盖
credit 环），两种配置都饱和在链路/路由极限上，变体A 预测**平局**。
默认 1 周期链路下 BDP ≈ 2–3 ≤ r+P/2 = 4：只在默认链路跑实验会**按构造复现
零结果**——正是阶段2 揭示的那类陷阱。实验必须用长链路制造 C1（§6），而长
链路同时就是诚实的应用域：chiplet / D2D、片上长互连——credit RTT 随距离和
SerDes 延迟增长、每 lane 缓冲面积金贵，与许可域模型（`permission_domain/`
§3）的 PS1 域重合：长 credit 环让令牌和槽同时稀缺，DP-Perm 浮动令牌划分，
DP-Phys 浮动槽位划分。

**C2（对内需求不对称）**：只有对向闲时才有槽可借。tornado 类流量加热一个
方向、闲置对向，是结构性 C2；xopposite 双向皆热，是结构性 ¬C2。

**结论：收益 ⟺ C1 ∧ C2；侵占暴露 ⟺ C1 ∧ ¬C2。** 两句都可证伪，实验矩阵在
两侧都布了格子。

---

## 4. 代价（三类，全部明示）

### 4.1 硬件与实现代价（gem5 不建模 → 测得收益是上界）

池化背后的可实现结构是 DAMQ 式共享 SRAM + 每 VC 链表
**[未验证: DAMQ 属 Tamir & Frazier 1992 经典结构，原文未取]**；共享 SRAM
路由器缓冲的可造性由 Fukase 等人的实现背书 **[已验证: 阶段2 事实核查
`research/factcheck/`——多输入端口共享缓冲，面积动机]**。具体账单：

1. **指针存储**：每槽一个 next 指针（`⌈log₂B_pair⌉` 位，B_pair=8 时 3 位），
   每 VC 头尾指针，加空闲链表。相对 128+ 位的 flit 属个位数百分比，小但非零。
2. **写端口冲突**：E、W 可能同拍到达，同写一块 SRAM——要么双写口
   SRAM（面积/功耗显著上升），要么插入仲裁串行化（Fukase 的正题），后者在
   同拍到达时引入额外延迟。私有缓冲天然无此问题。
3. **走线与时序**：E、W 端口在路由器两侧，共享缓冲必须居中放置——端口到
   缓冲的走线比各自贴身的私有缓冲长，高频下有时序压力。
4. **授予环路（池 credit 的动态分发）**：池 credit 须由接收方在两个上游
   之间动态划分，三种策略各有代价——急切授予有"credit 停泊"（授给 E 而
   E 不用的份额 W 也用不了，除非加召回）；懒惰授予每次借用多付一个
   请求-授予往返；混合（常驻小浮动+按需补充）居中。授予环路有延迟 ⇒
   上游看到的池状态永远是旧的（= 许可域 H3 陈旧性问题的物理来源）。
   可实现性有字面工业先例：UPI 的 VNA（共享池 credit，接收方动态授予）+
   VN0（静态保留 credit）双计数结构 [已验证: `research/iface_digest.txt`]，
   发送方本就维护两套计数、先池后保留消费——与本设计逐项对应。
5. **gem5 定位**：以上四项 gem5 全部不建模；全局注册表等价于**零延迟的
   完美授予者**（上游瞬时读到池的真实余量）。因此本实验测的是该机制在
   授予策略最优极限下的**性能上界**；面积/时序账由 [已验证] 的 Fukase
   先例、协议可行性由 [已验证] 的 UPI 先例分别承担，报告中必须如此声明。

### 4.2 干扰代价（C1 ∧ ¬C2 时的结构性暴露）

无治理（`c=P`）时对向可把池抽干，`f` 的容量从私有的 `r+P/2` 跌到 `r`——
方向间吞吐不公平与 matched-load 延迟退化是预期现象（经典 DAMQ hogging）。
cap 是治理旋钮，但**治理本身有价**：floor 每抬 1 槽，可借上限就降 1 槽
（最大增益 = `c − P/2`）。`c = P/2` 是零退化零收益的中性点。增益与保护
不可兼得，只能选点——这正是实验要测的剂量曲线。

### 4.3 适用域与复杂度代价

- **窄适用域**：C1 在 1 周期链路的普通 mesh 里通常不成立——在那里部署此
  机制是**只付 4.1 的账、收益恒为零**。机制只该部署在长 credit 环或极小
  缓冲的场合，这是设计结论的一部分而非遗憾。
- **正确性负担**：私有缓冲的隔离是物理免费的；池化后"逃逸 flit 永远落得进
  保留区"要靠分配逻辑保证（保留区准入规则，见 Q1），验证面变大。

---

## 5. gem5 / Garnet 实现要点

基座：routing 4（自适应+逃逸），Torus3D，ctrl vnet 0（1-flit 包）——阶段2
PB/PC 同款。阶段1 的 DP 计数器已经很接近（对共享计数 +1/−1、保留豁免、门
在 `SwitchAllocator::vc_allocate`），改三处语义即成变体A：

1. **驻留记账替代窗口记账**：计"本侧超出保留区的驻留 flit 数" `u_f`，不再计
   "共享编号段的 VC 分配数"。保留区满后，新分配门条件
   `u_f < c ∧ u_f + u_{f′} < P`。+1/−1 挂点不变（分配授予 / credit 返回），
   约 30 行。
2. **深 VC**：`buffers_per_ctrl_vc = r + P`，使单 VC 深度永不先于池约束触底；
   物理诚实由不变式 PHY 经计数器强制（模拟器超配每 VC 数组，记账把每对总
   驻留压回 `B_pair`）。**检查项 V1**：确认 Garnet 允许输出 VC 在下游仍有
   驻留 flit 时（credit 允许）被重新分配给新包——预期可以，未查实前不得
   信任深 VC。
3. **旗标与统计**：`--dpphys-cap`；新增 `dpphys_pool_full_blocks`、
   `dpphys_borrowed_peak`、**分方向接收吞吐**（公平性指标需要）。

4. **credit 语义（双货币结构）**：每-VC credit 是每条链路私有的——E 的
   flit 落池只动东邻居的计数器，**西邻居的每-VC credit 不动**；W 被堵是
   因为第二道门（池计数器）关闭，西邻居攥着 VC credit 发不出去，计入
   `dpphys_pool_full_blocks`。深 VC 下每侧广播的 VC credit 总量
   `v(r+P)` 远超 `B_pair`（按设计超售），每-VC credit 只保留编号占用/
   FIFO 职能，**容量约束整体由池计数器承载**。记账口径：+1 在分配授予、
   −1 在 credit 返回，故 `u_f` 是"已承诺槽"（发送→credit 归还全窗口），
   `L=8` 时按 BDP 膨胀——与真实硬件的 credit 承诺语义一致，且与 C1 自洽。
   真实硬件实现即双货币 credit 协议：每侧 `r` 个私有保留 credit + 接收方
   按需授予的 `P` 个池 credit——恰为 `permission_domain/design.md` §5 的
   **Option G**（接收方授予）。gem5 全局注册表把授予环路抽象为零延迟
   （Δ=0 神谕，测上界；真实授予环路的陈旧性 = 许可域 H3 问题，两条研究线
   在流控协议层重合）。

   **与传统 credit 的语义对照**：传统 credit 一枚令牌同时承载三职——
   存储保证、队列准入、静态所有权——且单枚充分可发。本设计将三职拆分：

   | 属性 | 传统 credit | 保留 credit | 每-VC credit（深VC后） | 池 credit |
   |---|---|---|---|---|
   | 指代 | 下游定 VC 的一个真实槽 | 保留区真实槽 | VC 队列位置，不保证存储 | 池中不定名槽 |
   | 发送充分性 | 单枚充分 | 单枚充分（逃逸） | 必要不充分 | 必要不充分 |
   | 所有权 | 静态属单一上游 | 静态 | 静态（超售 `v(r+P) > B_pair`） | 动态，两上游间迁移 |
   | 守恒范围 | 每(链路,VC) | 每侧 r | 无物理守恒（gem5 深VC 伪影；真实 DAMQ 无每-VC 深度计数） | 跨两条链路守恒 P |

   自适应 flit 发送 = 双钥匙（每-VC credit ∧ 池 credit）；逃逸子系统完全
   传统（逃逸 VC 的每-VC credit 即保留 credit，1:1 绑真槽），死锁证明因此
   逐字迁移。UPI VNA 打破类绑定（单链路内无记名池，[已验证]）；本设计把
   无记名推到**跨两条链路**——偏离传统的那一步 = 空间范围新颖性所在的那
   一步，也 = 全部代价（停泊/陈旧/双钥匙门）的来源。非传统性即机制本身。

长链路：链路延迟是 Torus3D 拓扑参数，`L ∈ {1, 8}` 给出 BDP ≈ 3 与 ≈ 17+，
分别落在 ¬C1 与 C1。

---

## 6. 实验矩阵与预注册预测

4×4×4 Torus3D，vnet 0，10 000 周期，速率 0.05–1.00 步长 0.05，五模式
（xopposite、tornado、transpose、neighbor、uniform_random）。复用阶段2
harness（`research/run_sweep_physical.py` / `analyze_physical.py`），换模式表。

| 模式 | v | 槽/对 | 机制 | 用途 |
|---|---|---|---|---|
| PRIV | 4 | 8（4+4 私有） | 无 | 基线 |
| PHY-c4 | 4 | 8（r=2, P=4, c=4） | 池，无治理 | 最大增益/最大侵占 |
| PHY-c3 | 4 | 8（r=2, P=4, c=3） | 池，有 cap | 剂量点 |
| PHY-c2 | 4 | 8（r=2, P=4, c=2） | 池，floor=私有 | 零退化点 |
| PHY-p0 | 4 | 8（r=4, P=0） | 池关闭 | 锚点 A0 |

× `L ∈ {1, 8}` → 10 格 × 5 模式 × 20 速率 = **1000 次运行**。
（第二 family（v=3，6 槽/对）视首批结果再加，PC 式余量保险。）

**预测（见数据前固定，阶段2 规则；赢须可复现，零结果照实报）：**

- **P1（零对照，¬C1）**：`L=1` 时所有 PHY ≈ PRIV，每个模式都平。此处出现赢
  说明模型有错——其信息量与 `L=8` 的赢等同。
- **P2（增益，C1∧C2）**：`L=8` tornado（及 transpose/uniform 的定向负载格）：
  PHY-c4、c3 饱和吞吐胜 PRIV，效应有序 `c4 ≥ c3 ≥ c2 ≥ PRIV`。
- **P3（侵占，C1∧¬C2）**：`L=8` xopposite：PHY-c4 出现分方向不公平和/或
  matched-load 延迟退化；PHY-c2 消除退化。若 cap 在此严格同时胜过 PHY-c4
  与 PRIV——那是全项目第一个 cap 正收益，显著报告。
- **P4（锚点）**：PHY-p0 ≡ PRIV，两个 L、全部 100 速率点位级一致（代码路径
  关闭时惰性）。
- **P5（死锁）**：1000 次运行 0 死锁（逃逸基座按构造未动，定义 5）。

**指标**：饱和吞吐；matched-load 延迟（接受 ≥0.99、送达 ≥0.95，阶段2 定义）；
xopposite 下分方向吞吐 min/max 比（公平性，新增）；`dpphys_borrowed_peak`
（借用是否真实发生——机制启用检查，防"因惰性而平"误读为"无害"）。

---

## 7. 与前两阶段及先行工作的关系

**三阶段分解**：`naive 增益 = 编号效应 + 槽效应 + 约定效应`。
阶段2 固定编号与槽，测得**约定效应 = 0**。阶段1 同时变编号和槽，其增益是
编号并行度/HOL 缓解与额外存储的未分解混合。**变体A 固定编号、池化槽，
分离出槽效应**——并附带 Prop（§3）的预测：¬C1 下槽通道惰性。顺带一个
诚实的再解读：在 DAMQ 硬件假设下，阶段1 的等承诺约定恰好是**等槽**规则
（每对需 `2r+S` 槽，与配对 plain 相等，模式表
`naive-research/run_sweep_dp.py:57`）——但它从未声明该硬件、未控制编号数
差异、且跑在槽通道惰性的 `L=1` 上（其增益的来源更可能是编号通道）。旧数据
可作暗示引用，不可为变体A 作证；变体A 的主张由自己的 1000 次运行承担。
（未来的"编号富余"变体——每侧编号多于槽、DAMQ 背书——可分离编号效应，
补完分解；仅记为后续工作。）

**先行工作**：物理缓冲共享本身是成熟方向——DAMQ **[未验证, 经典]**、
Fukase 共享 SRAM 路由器 **[已验证]**、UBMR 的 E-W/N-S 合并省面积
**[未验证: 摘要级]**、bubble 共享类流控 **[未验证]**。主张因此**不是**
"发明了缓冲共享"，差异化三点：

1. **共享范围 = 对向端口对，绑维度语义**——共享布在需求反相关有结构保证
   （C2）的地方，而非为省面积的全路由器合并。
2. **死锁基座保留区隔离**——逃逸网络整体活在永不池化的保留区，安全证明
   零改动（定义 5）；面积动机的合并工作不做携证明的保留区。
3. **治理即研究对象**——cap 剂量响应、floor 保证、公平性指标、预注册零
   结果，全套继承阶段2 方法论，对准一个物理真实存在的池。

---

## 8. 待定问题

- **Q1** 保留区粒度：**已定（双货币视角拍板）**——逃逸 flit 只用保留
  credit/槽，自适应 flit 只用池 credit/槽。逃逸永远落得进保留区（无自适应
  争抢），死锁证明零条件成立；`u_f` 直接定义为"本侧池承诺数"，无滑动豁免
  口径的歧义。推论：`r` 取逃逸 VC 深度（PB/PC 基座下 r=1/侧），实验矩阵的
  r、P 数值需相应调整（如 r=1、P=6 对齐 PB 布局）——实现前最终确定。
- **Q2** `L=8` 是否需要加大 `sim-cycles`/预热（流水线变长，延迟统计收敛慢）。
- **Q3** 第二 family（v=3，6 槽/对）现在加还是首批结果后加。
- **V1** 代码核查：输出 VC 带下游驻留 flit 的重分配（§5.2）。

---

## 9. 符号表

| 符号 | 含义 |
|---|---|
| `B_pair` | 对向端口对的物理槽总数（= `2r + P`） |
| `r` | 每侧私有保留槽（逃逸基座） |
| `P` | 每对共享池槽数 |
| `u_f(t)` | `f` 侧池占用 `max(0, occ_f − r)` |
| `H_f(t)` | 动态落地上限 `r + P − u_{f′}(t)` |
| `c` | 方向 cap，`u_f ≤ c` |
| `floor_f(c)` | 被侵占到顶时的保底容量 `r + P − c` |
| `C1` | 缓冲瓶颈条件 `BDP > r + P/2` |
| `C2` | 对内需求不对称 |
| `L` | 链路延迟（gem5 中制造 C1 的旋钮） |
